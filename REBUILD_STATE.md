# Matthew Rebuild — Rolling State

This is the single rolling state file for the Matthew rebuild. Update it in place at session-close; don't write per-session handoffs. The voice skill carries the framework spine; this file carries the rebuild-specific drift.

## Where we are

- **Done**: matt-1 through matt-28 (short.md + free.md for each). **THE MATTHEW SWEEP IS CLOSED — 28 OF 28.**
- **Round 1 ship — DONE (S110-S111):** Matthew commentary + full cross-reference apparatus live on `bible.remnantofpromise.org`. Specifically:
  - `chapter_intro` populated from matt-N-free.md for all 28 chapters (loader at `restoration-pipeline/_session110_load_matthew_free_tier_commentary.py`).
  - PWA italics fix (shared markdown helper at `app/src/lib/markdown.tsx`), full cross-reference verse text (no truncation), pericope-style paragraph breaks (KJV USFX-extracted paragraph_starts.json bundled in PWA, 4,635 markers across 65 books), "Members in this chapter" → "Cross-references in this chapter" label rename.
  - 1,695 curated cross_references rows + 67 threads + 1,123 Layer 1 rows materialized into prod via `data-schema/migrations/session110_matthew_cross_references.sql`.
  - matt-5 fulfill treatment + `fulfill-as-prophecy-bringing-to-pass` thread (anchor Matt 5:17-19; 35 NT plēroō prophecy-fulfillment members) shipped via `data-schema/migrations/session111_fulfill_thread.sql`.
- **Highlights wheel — DONE (S113):** 13-color × 3-style mark picker live on every verse of every canon chapter. Specifically:
  - Schema migration `session113_user_highlight_labels.sql` shipped: `verse_highlights` gained a `style` column with CHECK constraint (`fill` / `underline` / `outline`); unique constraint swapped from `(user_id, verse_id, color)` to `(user_id, verse_id)` so one mark per verse per partner (re-marking replaces); new `user_highlight_labels` table for the $1.99 free-form color-meaning dictionary (empty by default — symbolic tribe + breastplate-gemstone mapping is V2 enrichment per DESIGN_LANGUAGE.md §6, NOT preloaded in V1).
  - 5 new API endpoints: `GET /v1/highlights?book_slug=&chapter_number=`, `POST /v1/highlights`, `DELETE /v1/highlights/{id}`, `GET /v1/highlights/labels`, `PUT /v1/highlights/labels`. Tier-aware color + style validation: free locked to (neon_yellow, fill); $1.99-and-above unlocks all 12 tribe colors + neon_yellow × 3 styles = 39 configurations. PUT labels 403s for free (free-form dictionary is a $1.99 feature per §9). All endpoints auth-required.
  - Shared `upsert_user` helper extracted from subscriptions.py to db.py so highlights + future per-user surfaces (notes, reading-position, etc.) share one path.
  - New `get_current_user_required` dependency in auth.py — 401s anonymous callers; used by every write-route in the new wheel.
  - PWA: new `HighlightPicker.tsx` component (long-press 500ms / right-click opens picker as fixed-position modal). 13-swatch grid in canonical render order (neon_yellow → crimson → tangerine → honey → sage → emerald → teal → sky_blue → periwinkle → lilac → magenta → rose → parchment), 3-style toggle row, locked swatches dim + route to `/pricing` on tap. Optimistic UI on create-or-replace + delete. Inline label editor accessible via ⚙ gear (paid tiers) or upgrade CTA (free tier).
  - CSS: three mark classes in `index.css` — `.mark-fill` (translucent tinted background, ~33% alpha chromatic / ~22% parchment), `.mark-underline` (3px solid colored line + skip-ink: none for clean rendering through descenders), `.mark-outline` (4-direction `text-shadow` — same CSS technique as the sacred-name treatment per DESIGN_LANGUAGE.md §2, deliberate typographic family).
  - Mid-session correction (S113 mistake → recovered): initial Stop 2 proposed a 6-color framework-labeled palette; Yoshi flagged the miss; DESIGN_LANGUAGE.md §6 / §7 / §8 (locked S77/S78) carries the locked spec (12 tribe colors + neon_yellow free + 3 mark styles). Stop 2 rebuilt against the locked spec before any deploy. **Forward discipline: read DESIGN_LANGUAGE.md before proposing any visual / interaction spec — the spine carries S77/S78 locked decisions that REBUILD_STATE.md only summarizes.**
- **Tiered commentary surface — DONE (S112):** `matt-N-short.md` (Basic) + `matt-N.md` (Deeper Dive) now render below the free chapter_intro and above the cross-reference apparatus on every Matthew chapter. Specifically:
  - 56 `commentary_entries` rows live in prod: 28 Basic (`surface_kind='inline'`, `tier_required='study_notes'` per S112 fix) + 28 Deeper Dive (`surface_kind='deep_dive'`, `tier_required='extras'`). Loader at `restoration-pipeline/_session112_load_matthew_tiered_commentary.py`. Total: ~374k words of framework reading on Matthew.
  - **Tier-mapping correction** (S112): graduated reveal across the v1 ladder. **Free** = `chapter_intro` (S110). **Notes ($1.99)** = `chapter_intro` + Basic. **Library ($4.99)** = chapter_intro + Basic + Deeper Dive + full extras book library + BDB/Thayer's/Gesenius/Easton's/Smith's/ISBE/maps/atlases/concordances + Josephus. Earlier voice from S46/S47 placed BDB+apparatus at free; S112 moved them to Library per Yoshi's call so the $4.99 tier earns its keep on tools.
  - New API endpoint `GET /v1/books/{slug}/chapters/{n}/commentary` returns commentary_entries with tier-gating per row — locked rows come back with body=null + locked=true so the PWA renders an upgrade affordance with "Unlock with [Tier] ($X.XX/mo)" CTA (Yoshi's "catch the eye" pattern A).
  - New PWA component at `app/src/components/ChapterCommentary.tsx` renders the stacked Basic / Deeper Dive blocks with collapsible expanders ("Read the basic walk →" / "Read the deeper dive →"). Hide-commentary toggle persists per-user via localStorage. matt-N.md `## §N` sub-sections render as h5 sub-headings inside the Deeper Dive body.
  - "← Back to Remnant of Promise" link added to the PWA footer.
  - **Future-loader pattern established (S112):** post-seed loaders ship inside the API Docker image (Dockerfile COPY + .dockerignore negate-pattern) so they run from Render Shell — sidesteps the external-Postgres SSL/IP rejection that blocked the laptop path. Use this for all future loaders.
- **Subscription-state sync drift fix — DB-wins-over-JWT DONE (S114):** auth.py now re-resolves `partner_tier` from the `subscriptions` table on every authenticated request and overwrites the JWT claim with the DB value. The WP `rop_partner_tier` user-meta drift that surfaced in S113 is now harmless to API correctness — the API trusts the DB, not the JWT. Specifically:
  - `api/auth.py` — new `_resolve_tier_from_db(wp_user_id_str)` helper queries `SELECT s.tier FROM subscriptions s JOIN users u ON u.id = s.user_id WHERE u.wordpress_user_id = $1 AND s.status NOT IN ('canceled','unpaid','incomplete_expired') ORDER BY s.started_at DESC LIMIT 1`. NULL → 'free'. Terminal-status list matches `_handle_subscription_change` in subscriptions.py exactly. Transient DB failure → fall back to JWT claim (graceful degradation, no partner kicked to free by a flaky pool). `get_current_user_optional` calls it after `_decode_token` and overwrites `user.partner_tier` via `model_copy(update=...)`. Every downstream `user_tier(current_user)` call site (10+ across main.py) gets DB-correct tier transparently.
  - Verified live: Yoshi's account (DB row at `everything`, WP user-meta still at `free` from historic drift) now sees all paid books and the paid-tier picker works. The DB-wins fix eliminates the chrome-vs-API mismatch that 403'd him in S113 testing.
  - **Two mistakes caught + corrected mid-session:**
    1. First commit used `from .db import get_pool` — relative import that fails in the flat-module layout the codebase uses (`from auth import ...` in main.py, not `from .auth`). Fixed to `from db import get_pool` before any partner ran into it; only cost was the second commit.
    2. First reconciliation script ORDER BY referenced `s.created_at` which doesn't exist on the subscriptions table — the right column is `s.started_at` (NOT NULL DEFAULT now() at row insert per schema.sql line 493). Auth.py had the SAME bug; both shipped with the wrong column. Caught when the reconciliation dry-run threw `asyncpg.exceptions.UndefinedColumnError`. The auth.py path was gracefully degrading to JWT claim per the exception fallback — which meant the fix wasn't actually winning anything in prod until the started_at fix landed. Both files fixed and shipped in commit two.
  - **Reconciliation script blocked by Cloudflare bot mode (NEW next-wheel, see below).** The S114 reconciliation script (`restoration-pipeline/_session114_reconcile_partner_tiers.py`) was supposed to walk the subscriptions table and re-fire `_sync_partner_tier_to_wp` for every partner with their canonical tier. Dry-run succeeded; apply hit a Cloudflare 403 with a JS challenge page body (`__CF$cv$params` snippet in the response). Cloudflare's bot protection is challenging the Render → WP POST. Script ships in the API Docker image per the S112 pattern and stays runnable for when the Cloudflare path is fixed; harmless to leave un-run because the auth.py fix means WP drift no longer affects API tier resolution.
- **Resubscribe CTA in chrome + Manage.tsx — DONE (S115 Wheel 1):** Closes the chrome-side hole that left canceled / past_due / unpaid / incomplete partners with no door back from the header. Pairs with S114's auth.py DB-wins fix — S114 made the API DB-correct, S115 gave canceled-and-related-status partners a visible path back to /pricing from both the reader chrome and the Manage page itself.
  - `app/src/App.tsx` lines 257-296: chrome ternary broadened to four branches across the full `SubscriptionStatus` set (none | trialing | active | past_due | canceled | unpaid | incomplete | incomplete_expired per `app/src/lib/api.ts` line 342-350). `active || trialing` → Manage; `none` → Become a partner (filled-primary); `canceled | past_due | unpaid | incomplete | incomplete_expired` → Resubscribe (filled-primary, identical styling to Become-a-partner so the chrome reads as one button family with state-dependent label). Defensive null branch is now unreachable in practice — the type system covers all 8.
  - `app/src/routes/Manage.tsx` lines 356-403: winding-down banner gets a Reactivate button under the resubscribe-anytime paragraph; terminal banner keeps the existing sentence-with-inline-link verbatim per Yoshi's call and adds a Resubscribe button below it. Both buttons reuse the existing filled-primary classNames — no new visual register.
  - **Spec path drift caught at-read-time:** S114 spec named `app/src/components/Manage.tsx`; actual path is `app/src/routes/Manage.tsx`. Off-by-one on line numbers too (357-377 → actual 356-377; 380-388 → actual 379-388). Forward rule reinforced: when a spec names a path or line range, READ the actual file before submitting edits. The S114 discipline is paying for itself.
  - Verified live: Yoshi's account in `active + cancel_at_period_end=true` state showed the Manage chrome button (active branch routing intact) AND the Reactivate button on the winding-down banner (new B1 affordance rendering cleanly). Terminal-banner Resubscribe + chrome Resubscribe branches use identical code patterns to the verified Reactivate; confirmed by code-pattern identity since Yoshi's state couldn't surface them directly.

- **Dark-default theme + chrome toggle + Cormorant body — DONE (S115 Wheel 3, font weight follow-up pending verify):** Ships the locked DESIGN_LANGUAGE.md §1 signature aesthetic (black `#000` bg + white serif body) that the live PWA was contradicting. Closes the "PWA loads in light mode" gap surfaced in S114. Specifically:
  - `app/src/index.css` — `:root` carries the dark default set (`color-scheme: dark`, `--reader-bg: #000`, `--reader-text: #fff`, `--reader-muted: #a3a3a3`, `--reader-rule: #2a2a2a` generic borders, `--reader-surface: #0a0a0a` cards/dropdowns, `--reader-accent: #0084ff` for chapter divider + verse numbers per §5). `[data-theme="light"]` selector carries the off-ramp parchment/sepia set. New `--reader-surface` variable replaces hard-coded `bg-white` across the JSX. New `--reader-accent` variable carries the §5 chrome accent — Yoshi elected 470nm spectral blue (#0084FF). `.verse-number` class flipped from `--reader-muted` to `--reader-accent` at 0.85 opacity. `.swatch` border switched from hard-coded rgba to `var(--reader-rule)` so it stays visible in dark.
  - `app/index.html` — inline `<script>` in `<head>` reads `localStorage.getItem("reader-theme")` and applies `data-theme="light"` to `<html>` before paint. Default with no preference = dark. Prevents FOUC for stored-light-mode partners. Google Fonts preconnect + Cormorant Garamond stylesheet link with `display=swap` (weights 400/500/600/700 + italic 400 imported).
  - `app/src/lib/theme.ts` (NEW) — `useTheme` hook: reads `document.documentElement.dataset.theme`, exposes `toggle()` + `set()`, persists to localStorage under key `reader-theme`. Mirrors the hide-commentary toggle persistence pattern from `ChapterCommentary.tsx`.
  - `app/src/App.tsx` — new `ThemeToggle` component (☼/☾ icon-button) inserted into chrome header to the left of the subscription CTA. Wraps the subscription ternary in a `<div className="flex items-start gap-2">` so toggle + CTA cluster on the right. Chapter-header divider border flipped to `--reader-accent` per §5. `bg-white` (3 refs in this file) → `bg-[var(--reader-surface)]`. `text-white` on the Become-a-partner + Resubscribe filled-primary buttons → `text-[var(--reader-bg)]` so they stay inverted-contrast across both themes.
  - Across `Manage.tsx`, `Pricing.tsx`, `HighlightPicker.tsx`, `ChapterCommentary.tsx`, `ChapterEndCard.tsx`: `bg-white` → `bg-[var(--reader-surface)]` (17 refs); `text-white` paired with `bg-[var(--reader-text)]` → `text-[var(--reader-bg)]` (13 refs). Amber/red warning + error banners intact (intentionally theme-independent). Pricing.tsx amber-700 CTA button text-white intact (paired with amber bg).
  - **Cormorant Garamond mirrors the website's locked Royal & Priestly palette typography** (website `css/main.css` line 30 declares `--serif: 'Cormorant Garamond', ...`). Cross-surface typographic unity.
  - **Website-vs-PWA theme delta is intentional:** website uses deep parchment-brown `#14100c` (locked S13); PWA uses pure black `#000` per §1's "brand-defining signature" spec. NOT a discrepancy.
  - **Website has no theme toggle** — confirmed by reading `~/Desktop/Website/static-site/css/main.css`. The website is dark-by-default with no off-ramp. The Bible PWA's toggle is per §1's "alternate available in settings" spec, not a website-mirror move.
  - **Font weight + size follow-up staged but not yet deployed at session-close:** Yoshi reported Cormorant @ 400 weight + 16px body reads anemic and slightly blurry. Patch staged in `app/src/index.css` (new `body { font-weight: 500; font-size: 18px; }` rule); commit message + push instructions prepped, awaiting Yoshi terminal-side push next session-open. If 500 weight still reads thin, EB Garamond is the standby (same family, screen-optimized).
  - **Saturation observations (deferred, second-opinion pending):** Yoshi observed (a) the 470nm spectral blue accent on chapter divider + verse numbers reads pastel rather than vivid, (b) emerald `#4DAE7F` in the highlight palette also reads pastel. Both deferred pending Yoshi's second-opinion check before any palette / accent re-pick. Possible follow-up wheel if confirmed.

- **Lora body font swap + line-height lock — DONE (S116):** Closes the S115 deferred font follow-up after fresh-eyes comparison on the actual black reader pane (techelet / argaman sacred-name outlines + 470nm verse-number accent + Hosea 1:9-10 sample passage). Cormorant @ 500/18 still read thin against pure black; Lora won the comparison against EB Garamond, Crimson Pro, and Spectral on stroke-weight-holding-the-outline + body-paragraph color. Specifically:
  - `app/index.html` — Google Fonts preconnect + stylesheet link swapped from `Cormorant+Garamond` to `Lora` (same weights: 400/500/600/700 + italic 400). Comment block updated to record the rationale + the intentional break of PWA-vs-website typographic unity (website stays Cormorant per S13 Royal & Priestly palette; PWA goes Lora because it's the long-form-reading surface where legibility wins over palette-mirror).
  - `app/src/index.css` — `font-family` at `html, body, #root` swapped from `"Cormorant Garamond"` to `Lora`; body rule augmented from `font-weight: 500; font-size: 18px;` to `font-weight: 500; font-size: 18px; line-height: 1.7;` (line-height explicit-locked, was previously inheriting Tailwind preflight's 1.5). Header comment + body-rule comment both updated to record the swap + the known Tailwind-override gap on chrome surfaces.
  - **Cross-surface typographic unity intentionally broken.** S115's "Cormorant mirrors the website's locked Royal & Priestly palette" stance is overridden at S116. The website (`~/Desktop/Website/static-site/css/main.css`) stays on Cormorant Garamond per S13. The PWA goes Lora because the PWA is the long-form-reading surface where legibility wins over palette-mirror. Documented in both the `index.html` comment and the `index.css` header comment so future sessions don't accidentally re-mirror the website font.
  - **Comparison-widget pattern paid off again.** Same as S115's color-comparison and highlight-palette widgets — when a visual decision is in scope, render a comparison widget instead of describing options in prose. The S116 widget showed all 5 candidate fonts (Cormorant Garamond, EB Garamond, Crimson Pro, Spectral, Lora) at controllable weight × size × line-height with the actual sacred-name outlines and verse-number accent rendered live on a true #000 reader pane, making the decision a single look + click instead of multiple rounds of conversation.
  - **Two new observations surfaced mid-deploy + captured into Next wheels (below):** (a) the 500/18 body register only lands on scripture text — chrome surfaces (notes, commentary, picker chips, dropdowns) stayed at 400/16 because their Tailwind utility classes override the body default; (b) refresh always returns to Genesis 1 — no reading-position persistence. The `reading_positions` schema table exists; wheel is the API + PWA hook.

- **Multi-mark highlights + UX touch-ups — DONE (S117 second wheel):** Closes the S114 Yoshi ask — *"i wanted it to be able to underline outline and highlight the same verse, and would like the ability to underline more than once in a different color, possibly three times"* — and bundles three small UX fixes Yoshi flagged mid-session (theme toggle discoverability, picker Labels copy, inline-pricing impression). Schema migration + API + PWA picker rebuild + verse render layering, all in one ship. Specifically:
  - **Schema (`data-schema/migrations/session117_multi_mark.sql`):** drop the S113 `verse_highlights_user_verse_unique` constraint, add `verse_highlights_user_verse_color_style_unique UNIQUE (user_id, verse_id, color, style)`. Same exact (color, style) tuple on the same verse is unique — re-applying is a no-op; different (color, style) combos coexist. Migration is idempotent (DROP IF EXISTS + DO-block guard on the ADD). Ships inside the API Docker image per the S112 pattern, run from Render Shell. Also fixed the source-of-truth at `data-schema/schema.sql` lines 583-590 — the `style` column (S113) was never reflected there and the constraint was still at the original pre-S113 shape; that block now matches the migration outcome (style column with CHECK constraint + named multi-mark unique).
  - **API (`api/main.py` POST /v1/highlights):** `ON CONFLICT ON CONSTRAINT verse_highlights_user_verse_unique` → `ON CONFLICT ON CONSTRAINT verse_highlights_user_verse_color_style_unique`. DO UPDATE is now effectively a no-op (EXCLUDED.color = existing.color and EXCLUDED.style = existing.style on conflict — RETURNING gives back the row unchanged). Docstring rewritten to record insert-or-no-op semantics + the cap-enforced-at-picker rule. GET endpoint unchanged (already returns flat array).
  - **PWA `HighlightPicker.tsx` (complete rebuild):** chips-row at top showing existing marks (color dot + style label + × to remove each); cap-reached banner replaces the swatch/style picker when `current.length >= 3`; "Mark verse" button (or "Add mark" copy when marks already exist) disables when the selected (color, style) is already on the verse with "Already marked" copy; per-chip × handler calls `deleteHighlight(mark.id)` + `onDeleted(mark.id)` and keeps the picker open so the partner can immediately add a replacement or remove another. Props changed: `current: Highlight | null` → `current: Highlight[]`; `onDeleted: (verseId: number)` → `onDeleted: (highlightId: string)`. The bottom-left "Remove mark" bulk button retired (chips handle deletion).
  - **PWA `App.tsx`:** `highlightsByVerse` type changed from `Record<number, Highlight>` to `Record<number, Highlight[]>`. Chapter-load `useEffect` buckets marks into arrays via `(map[h.verse_id] ??= []).push(h)`. Verse render layers 0..3 marks via nested spans — each mark wraps the inner content with its own `mark-{style}` class + per-color CSS variables. Multi-underline stacking: for each underline mark, `textUnderlineOffset` computed from underlineIdx (2px, 7px, 12px) so multiple colored underlines render as distinct stacked lines instead of overlapping. Picker callbacks updated: `onSaved` appends + dedup-filters on (color, style) tuple (handles insert-or-no-op API return); `onDeleted` filters by `mark.id` across all verse arrays and drops empty arrays.
  - **DESIGN_LANGUAGE.md §8 update:** new paragraph documenting multi-mark + 3-mark cap. Notes that earlier sessions never explicitly said "one mark per verse" — that was an S113 implementation choice. S117 lifts it; the §8 update makes the new behavior the locked design.
  - **UX touch-ups bundled into the same commit:** (1) **Theme toggle visible label** — ThemeToggle in App.tsx now renders `[☼/☾ glyph] Theme` instead of glyph-only. Yoshi observed the icon-only button was hard to find; glyph stays as the visual hook, "Theme" makes the affordance discoverable. (2) **HighlightPicker Labels copy** — paid-tier button text changed from "⚙ Labels" to "+ Add color labels" (clearer call-to-action; partner now knows what the gear does). (3) **Removed the inline "— $1.99" tag on the Labels button** — the free-tier Labels button is hidden entirely. Yoshi's observation: a single button labeled "Labels — $1.99" created the impression that every feature was individually-priced. The general upgrade prompt at the bottom of the picker still explains what $1.99 unlocks; removing the inline-priced surface eliminates the every-action-priced feel without losing the upgrade-CTA path.
  - **Yoshi gates settled this session:** (a) hard cap at **3 marks** per verse — matches "2-3 stack reads clean" guidance; (b) **close modal on commit** — partner long-presses again to add another mark; matches the single-mark mental model and stays clean for the common case.
  - **Deploy ordering note.** This wheel introduces a schema dependency: the new API code references the new constraint name in ON CONFLICT, so it requires the migration to have run. Sequence: deploy the API+PWA build, then Yoshi runs `psql $DATABASE_URL -f /app/data-schema/migrations/session117_multi_mark.sql` from Render Shell. Brief window between deploy and migration where POST /v1/highlights returns 500 (~1-2 minutes) — acceptable for a low-traffic endpoint. The migration is one transaction so the constraint swap is atomic from the DB's perspective.
  - **Verification:** `tsc -b` clean across the entire project — no TypeScript errors introduced by the type changes (Highlight | null → Highlight[], onDeleted verseId → highlightId, nested-span verse render). Vite build couldn't run in the sandbox (same rolldown native binding issue as the chrome sweep — sandbox is Linux x64, Yoshi's node_modules installed on macOS); Render's fresh Linux install builds cleanly. tsc-clean is the load-bearing verification.

- **Tailwind chrome-register sweep — DONE (S117):** Closes the body-font-register gap surfaced at S116. The Lora 500/18/1.7 body bump was only reaching scripture text (and the verse text DIV's own `text-[1.05rem] leading-relaxed` override); commentary, banners, blurbs, snippets, cross-reference rows, and modal descriptions all carried Tailwind utility classes (`text-sm`, `text-base`, `leading-relaxed`) that beat the body default. The sweep propagates body register to body-prose chrome surfaces while leaving control chrome (buttons, badges, picker chips, form labels, status indicators) intentionally at its smaller affordance register. 22 edits across 6 files. Specifically:
  - `app/src/index.css` — body-rule comment updated to record the sweep landing + the architectural rule (body-prose chrome → body register; control chrome → stays at affordance register; verse text intentionally untouched as the already-shipped S116 register).
  - `app/src/components/ChapterCommentary.tsx` (3 edits) — card titles `text-base font-semibold` → `text-lg font-semibold` (sit above body in the card hierarchy); commentary body div dropped `text-[1.025rem] leading-relaxed` overrides so body 18/500/1.7 inherits on the actual reading prose (was 16.4px @ 1.625); locked-CTA paragraph `text-sm` → `text-base`. Sub-heading H5 (`text-sm font-semibold uppercase`) stays at chrome-label register intentionally.
  - `app/src/components/ChapterEndCard.tsx` (4 edits) — baseline list `space-y-3 text-sm` → `space-y-3` (snippet text inherits body); thread title `text-base` → `text-lg`; thread summary dropped `text-sm` (body inherits, `leading-relaxed` kept for card density per Yoshi's call); member-rows UL dropped `text-sm` (snippet inherits body). Chrome labels (`text-xs` panel header, verse-number labels, arrow glyphs, Anchor:, Show more) stay at chrome-label register.
  - `app/src/App.tsx` (2 edits) — chapter sub-title `text-sm italic` → `text-base italic`; chapter_intro Commentary aside dropped `text-sm` (body inherits, `--reader-muted` color preserved). Verse text DIV at L581 NOT touched — already-shipped S116 register, outside chrome sweep scope.
  - `app/src/components/HighlightPicker.tsx` (2 edits) — Color meanings modal title `text-base` → `text-lg`; modal description `text-sm` → `text-base`. "Mark verse" picker title (`text-sm uppercase tracking-wide`) and style-toggle chips (`text-xs`) stay at chrome-label register intentionally.
  - `app/src/routes/Manage.tsx` (8 edits) — anonymous prose, no-subscription prose, just-cancelled banner, subscription summary dl, both cancel-confirm body paragraphs, winding-down banner, terminal-state banner: all `text-sm` → `text-base`. Buttons, status labels, error inline, page nav links stay at chrome-label register.
  - `app/src/routes/Pricing.tsx` (7 edits) — both page subtitle paragraphs, emerald already-active banner, amber reactivation banner, tier blurbs, tier bullets UL, footer wrapper: all `text-sm` → `text-base`. The footer wrapper bump cascades to both nested paragraphs via inheritance. Buttons, cadence toggle, founder card chrome, back-to-reader link stay at chrome-label register.
  - **Yoshi call on banner sizing:** confirmed text-base (16px) for compact-card banners and short-form CTA paragraphs rather than full body 18px. Banners and tier blurbs sit inside compact cards where full body register would feel oversized; text-base keeps card proportions clean while still reading as body register (not chrome control). 18px reserved for actual reading-length prose (commentary card body, thread summaries, baseline list snippets).
  - **Yoshi call on thread-card line-height:** confirmed leading-relaxed (1.625) preserved on the ChapterEndCard thread summary for card density. Size is the lever; line-height stays slightly tighter than body 1.7 because the thread summary is in a compact card, not a long-form reading surface. Yoshi's exact phrasing: *"making it a little larger"* — confirms the size bump is the main signal.
  - **Verification:** `tsc -b` clean across the entire project — no TypeScript errors introduced. All Tailwind class strings introduced (`text-base`, `text-lg`) are standard utilities. Vite build couldn't run in the sandbox (rolldown native binding architecture mismatch between Yoshi's macOS install and Linux x64 sandbox); Render's fresh install on its Linux runtime builds the bundle cleanly. tsc-clean is the load-bearing verification.

- **Reading-position persistence with verse-precise tracking — DONE (S116 second wheel):** Closes the "refresh always returns to Genesis 1" observation Yoshi surfaced mid-S116 Lora deploy. Free-tier feature per DESIGN_LANGUAGE.md §9. Schema was already in place (S77 design + S110 build); this wheel wires the API endpoints and the PWA reader. Specifically:
  - `api/models.py` — two new pydantic models (`ReadingPositionResponse`, `UpsertReadingPositionRequest`) carrying the slug/chapter/verse_number register. The opaque `verse_id` stays server-side; the PWA never sees it.
  - `api/main.py` — two new endpoints (`GET /v1/reading-position`, `PUT /v1/reading-position`), both auth-required, no tier gate (free-tier feature). Edition-scoped to canon (slug `'canon'`) mirroring the highlights endpoint pattern. PUT resolves (book_slug, chapter_number, verse_number) → verse_id and upserts on `(user_id)` PK with `ON CONFLICT (user_id) DO UPDATE`. 404 when the verse triple doesn't resolve.
  - `app/src/lib/api.ts` — two client functions (`getReadingPosition`, `putReadingPosition`) + matching TypeScript types.
  - `app/src/lib/reading-position.ts` (NEW) — persistence helper carrying `loadInitialPosition()` (API → localStorage → null resolution), `saveReadingPositionDebounced()` (immediate localStorage write + debounced ~1500ms API write — last-call-wins; rapid scrolls collapse to one API write), `cancelPendingSave()` for unmount cleanup, and a localStorage shape `{bookSlug, chapter, verseNumber}` keyed under `reading-position`.
  - `app/src/App.tsx` — wired the Reader with: (1) hydrate-on-mount useEffect that calls `loadInitialPosition()` and sets selectedBookSlug + selectedChapter + currentVerse + initialScrollVerse + the `hydrated` flag; (2) IntersectionObserver useEffect (gated on `initialScrollVerse===null` to avoid a verse-1 false-positive before the initial scroll lands) that observes every verse span via the new `data-verse-number` attribute and emits the topmost-visible verse to `currentVerse`; (3) scroll-to-saved-verse useEffect using `scrollIntoView` with a 50ms delay; (4) debounced save useEffect gated on `hydrated`; (5) book/chapter picker handlers that reset `currentVerse` to 1 so the picker change doesn't briefly persist the previous chapter's last-visible verse.
  - **Verse-precise tracking architecture.** Per Yoshi's expansion of the wheel scope, verse_number tracks the topmost-visible verse via IntersectionObserver. The observer's rootMargin clamps the intersection band to the upper half of the viewport so a partner reading at the bottom of the visible area doesn't accidentally save the verse below their gaze. Recreated each chapter-load (new DOM nodes). Maintains a Map<verseNumber, boundingRectTop> of currently intersecting verses; on every callback, emits the verse with the smallest top.
  - **Race condition closed pre-ship.** First draft had observer attaching immediately when chapterDetail loaded, which fires once with verse 1 (browser un-scrolled) BEFORE the scrollIntoView at +50ms. That brief window would persist verse 1 to localStorage before the scroll landed; a refresh inside the window would resume to verse 1 instead of the saved verse. Fix: observer attach is gated on `initialScrollVerse===null` (cleared only by the scroll-completion effect). Caught + closed before any ship.
  - **Anonymous + offline behavior.** Anonymous partners get a 401 from the API endpoints; the helper catches it silently and falls through to localStorage. Same path for network errors. Authenticated + offline behaves identically. Cross-device migration is implicit — every successful save writes both surfaces, so the first chapter-change after sign-in promotes the localStorage value into the API row.
  - **No schema migration.** `reading_positions` already at schema.sql lines 597-601 from S77. PK is `user_id`, FKs to `verses`. The endpoint joins to chapters + books + editions for the slug-resolution at GET time.

- **Anonymous-checkout unblock via /goshen/ direct route — DONE (S119):** Closes the S113-surfaced /login 404 that was stranding every anonymous-partner click on Pricing. Took a different path than the S118 specs proposed — instead of building a combined /account surface on the WP side, swapped Pricing.tsx to point directly at the existing /goshen/ login (the slug Yoshi had moved login to months ago for anti-spam protection, surfaced mid-S119 when he caught Claude's over-engineered "spec a WP-side redirect" approach). One file edit, S114 + S115 paths verified end-to-end live. Specifically:
  - `app/src/routes/Pricing.tsx` line 153-155: anonymous-checkout redirect URL swapped from `https://remnantofpromise.org/login?redirect=...` (404'd) to `https://remnantofpromise.org/goshen/?redirect_to=...` (working WP login). Query param renamed from `redirect=` to `redirect_to=` to match WordPress's native post-login bounce convention (WPS Hide Login or equivalent slug-mover wrapping `/wp-login.php` honors `redirect_to=` per the standard WP form). Comment block updated to record the S119 swap + reference the S118 /account spec as future work for the create-account half.
  - **Verified live end-to-end in incognito:** anonymous → `/pricing` → click Subscribe → bounce to `/goshen/?redirect_to=...` → sign in with WP creds → bounce BACK to `/pricing` signed in (rop_jwt cookie set at `.remnantofpromise.org` parent domain per S114 auth path) → Reactivate banner renders (S115 Wheel 1 chrome correct, `cancel_at_period_end=true` state from S30) → click Reactivate → Stripe Checkout opens cleanly. **First real verification of the entire acquisition-to-subscription pipeline since S30.** The S115 testing gap (paid-flow testing blocked by WP-meta drift at the time) is also closed by this verification — full path now exercises S114's DB-wins-over-JWT tier resolution + S115's chrome ternary across all 8 subscription states correctly.
  - **S118 work superseded for the /login half; preserved for the create-account half + the landing-page wheel.** The S118 specs (`S118_READ_THE_SCRIPTURES_PAGE_SPEC.md` + `S118_LOGIN_REGISTER_SURFACE_SPEC.md`) and the staged Pricing.tsx `/account` edit are no longer the path forward for closing the /login 404 — S119's /goshen/ direct route closed it. The /account combined-surface spec stays valid for the create-account-for-new-partners wheel when that becomes scope; the /read-the-scriptures landing page spec stays valid for the marketing-surface wheel. Both spec docs remain on disk as future-wheel documentation. The staged Pricing.tsx /account edit was overwritten by the /goshen/ edit; never pushed.
  - **Yoshi's correction mid-session sharpened the wheel.** The original S113 diagnostic on the URL was based on Claude reading Pricing.tsx + inferring the broken URL from code rather than confirming the address bar with Yoshi at the time. That inference (`/login` is the 404 URL) baked forward through S114→S118 as if confirmed. Yoshi caught the actual state mid-S119: the URL inference held up (still 404'd on live test), but the WP-admin work he'd already done — moving the login slug to /goshen/ for anti-spam — made the S118 build-from-scratch path unnecessary. **The lesson**: when a session inherits an issue from a prior session's diagnosis, re-verify the issue with a live test FIRST rather than building forward from inference. Also: when designing a fix path for a cross-system wheel, gate on "what's already in place on the other system" before specifying greenfield architecture.
  - **Simple-is-better discipline reinforced.** Yoshi's instinct was right ("it's pointing to login instead of goshen seems obvious"); Claude's reflex to translate query params + spec WP-side redirects + reason about middleware was over-engineering relative to the actual simplest path (change the URL Pricing.tsx points to). Forward rule: when the partner names a simple direct path that closes the bleed, take it and don't reason about the more complicated path the original spec proposed.

- **WP login + /account + Read-the-Scriptures landing page — DONE for the /login half (S118 specs shipped, S119 closed via /goshen/ direct route); CREATE-ACCOUNT + LANDING PAGE STAY QUEUED:** Closes the S113-surfaced /login 404 + missing create-account path acquisition gap, and adds a marketing-surface landing page that sells the V1 tier ladder. Scope expanded mid-session from "fix /login" to a full marketing-page + auth-surface rebuild after Yoshi requested a Read-the-Scriptures destination. Two spec documents shipped to repo root for Yoshi's WP-admin async build work; PWA-side edit staged + tsc-clean but NOT pushed until /account is live (would trade one 404 for another). Specifically:
  - **`S118_READ_THE_SCRIPTURES_PAGE_SPEC.md`** — WP page at `/read-the-scriptures` with menu label *Read the Remnant of Promise Official Study Bible*, H1 *The Remnant of Promise Official Study Bible*, subtitle *Restored sacred names. Apocrypha and pseudepigrapha. Commentary that reads the canon plainly.* Five sections: Hero, Primary CTA (Read in your browser → bible.remnantofpromise.org), Tier comparison sales section (three cards: Free / Notes $1.99 / Library $4.99 — $9.99 and $14.99 HIDDEN until scope defined per Yoshi's S118 call), Download section (App Store / Play Store / Mac / Windows / Linux all "Coming soon" disabled state — Capacitor wraps + PWA install button work still queued in Next wheels), Account note (Log In / Create Account CTA → /account). Pre-flight checklist + visual notes included.
  - **`S118_LOGIN_REGISTER_SURFACE_SPEC.md`** — combined Log In / Create Account surface at `/account` on WP. Five parts: (1) diagnose what's serving the /login 404 (WPS Hide Login / iThemes Security / Theme My Login / Solid Security plugin checklist + JWT-Authentication-for-WP-REST-API plugin integrity check); (2) /account page spec (combined form layout side-by-side, redirect-back contract honoring `?redirect=` query param with allowlist validation to remnantofpromise.org + bible.remnantofpromise.org hosts only, email verification ON per Yoshi's S118 call, password rules, error states); (3) Pricing.tsx redirect dependency; (4) Email deliverability fix — REQUIRED before /account ships (current WP setup delivers verification email ~1 hour after signup which kills the create-account UX; fix is WP Mail SMTP plugin + Google Workspace SMTP if Yoshi has Workspace email for info@remnantofpromise.org, else SendGrid free tier 100/day; bar is <60s delivery); (5) verification walk for SSO end-to-end.
  - **`app/src/routes/Pricing.tsx` line 153-155 — SUPERSEDED BY S119 + LIVE:** S118 staged an edit to `/account?redirect=...` (combined surface destination). S119 overwrote that staged edit with `/goshen/?redirect_to=...` (direct route to existing WP login) and pushed it live. The S118 /account edit was never pushed; the live deployed code carries the S119 /goshen/ edit.
  - **Marketing-surface voice rule locked (NEW S118).** The /read-the-scriptures landing page is conceptually back-cover/marketing copy, NOT body text. Per the voice skill's back-matter rule, restored sacred names do NOT appear on the landing page — only as a *feature description* in the Free tier card (e.g., "Restored sacred names — read the canon with the divine name restored to its original Hebrew"). Conventional English (God, Jesus, Israel) used throughout the page. Restored sacred names render INSIDE the app body text only, where the reader has committed to the journey. This is the first marketing-surface wheel in the rebuild work; the spec locks the pattern for any future marketing/landing/about-the-app surface.
  - **Decisions captured (verbal-answers-transcribe).** URL = `/read-the-scriptures` (WP page); menu label = *Read the Remnant of Promise Official Study Bible*; page H1 = *The Remnant of Promise Official Study Bible*; subtitle = *Restored sacred names. Apocrypha and pseudepigrapha. Commentary that reads the canon plainly.*; pre-ship app/desktop buttons = "Coming soon" disabled state with 60% opacity + `pointer-events: none`; login arch = combined Log In / Create Account on one `/account` page (NOT two separate /login + /register pages — Yoshi's *one login across everything* call interpreted as combined surface, since SSO across remnantofpromise.org ↔ bible.remnantofpromise.org is already wired via rop_jwt cookie at .remnantofpromise.org parent domain per S114 auth.py); login surface labeled "Log In / Create Account"; email verification = ON (Yoshi's call); tier comparison shows Free / Notes / Library ONLY (no $9.99 or $14.99 placeholders); tier card CTAs = Free → bible.remnantofpromise.org, paid → bible.remnantofpromise.org/pricing.
  - **Yoshi-side queue (in order).** (1) diagnose /login 404 + report plugin findings; (2) fix email deliverability + verify <60s test arrival; (3) build /account combined surface; (4) build /read-the-scriptures landing page incl tier comparison; (5) add menu item via Appearance → Menus; (6) ping me to push staged Pricing.tsx edit + walk Part 5 verification together.
  - **Tasks state at S119 close.** #1 (gates) completed S118; #2 (landing page spec) completed S118, build still queued as future wheel; #3 (login/register spec + diagnose) completed S118, build deferred — S119 closed the /login half via /goshen/ direct route, the /account combined-surface stays queued for the create-account-for-new-partners wheel; #4 (Pricing.tsx redirect) COMPLETED S119 — live pointing at /goshen/?redirect_to=...; #5 (verification SSO end-to-end) COMPLETED S119 — full incognito → /pricing → /goshen/ sign-in → bounce-back → Reactivate → Stripe Checkout flow walked end-to-end; #6 (email deliverability) pending — Yoshi-side WP-admin work, blocks the create-account wheel when that becomes scope but doesn't block anything currently live.

- **Range-selection architecture + multi-verse highlight — DONE (S123, Wheel 4 of the locked 14-wheel pre-launch sweep):** Shared range-selection mechanic + multi-verse highlight as the first consumer. Same architecture-now-beats-retrofit-later reasoning as S121 W3 menu architecture and S122 partner-tier resolution (third instance of the forward standard). The mechanic is the wheel; multi-verse highlight is the verification surface. Three known consumers across the sweep route through the one capture flow: W4 multi-verse highlight (ships this wheel), W7 multi-verse Copy with watermark (Coming-soon stub in the new RangeActionPicker), W7 range Share-with-watermark (Coming-soon stub in the same picker). PWA-only deploy. Specifically:
  - **DESIGN_LANGUAGE.md §21 written FIRST, code second** (post-S113 forward standard held). New section *Range-Selection Mechanic — Shared Across Multi-verse Highlight, Copy, and Share (locked S123, Wheel 4 of the pre-launch sweep)* with the full mechanic spec — one shared menu entry ("Start range here"), one capture flow, one branching action picker; same-chapter UX scope for W4 with the helper fully general for W7 inheritance; visual register (range-mode banner, anchor verse 2px spectral-blue left-border accent + 8% alpha tint, captured-range tint); action picker (header carries "Range captured — N verses" + spectral-blue range reference; three action items); HighlightPicker multi-target mode (hides chips, hides cap, "Mark N verses" button, parallel POSTs via Promise.allSettled with per-verse failure tolerance); §20 patches removing the now-obsolete S122 "Multi-verse range" Coming-soon stub from Share + updating the §20 stub catalog table to add the new Range section + correcting the count summary; §20 paragraph patch pointing at §21 for the locked mechanic.
  - **Yoshi's S123 gate settled the entry shape.** AskUserQuestion presented three real options: (A) action-specific menu items ("Highlight range" / future "Copy range" / "Share range"), (B) shared "Start range here" + second action picker, (C) gesture-only (long-press first, tap second). Yoshi's answer — *"why wouldnt we have the same shared click, unless we can explain to users the different gestures"* — locked Option B. One shared menu entry routing through one capture flow to one branching action picker; future Copy/Share at W7 flip from Coming-soon to Live in the same picker without changing the entry path. Cross-chapter scope: same-chapter only for W4 UX, helper fully general for W7. Settled with one AskUserQuestion gate per the spec-then-build discipline.
  - **`app/src/lib/range-selection.ts` (NEW).** Pure state machine + pure helpers — no React imports, no global state, no async. State shape: `{ status: 'idle' | 'selecting' | 'captured', start, end }` with verseRef carrying verseId + verseNumber + bookSlug + chapterNumber. Pure functions: `startSelecting(anchor)`, `commitEnd(state, end)` (auto-normalizes direction), `cancel()`, `isReversed(state)`, `isSameChapter(state)`, `isSameBook(state)`, `sameChapterRangeSize(state)`, `resolveSameChapterRange(state, chapterVerses)`, `orderRefs(a, b)`, `compareRefs(a, b)`. Helper supports cross-chapter + cross-book ranges via the state shape + comparator; W4 UX only exercises same-chapter (cross-chapter resolution lands as an injected async resolver when W7 needs it). compareRefs uses lexicographic bookSlug ordering as the cross-book fallback with the JSDoc explicitly noting the canonical-order gap — W7's cross-book consumer is expected to wrap with a witness_category canonical_order-aware comparator per §19.
  - **`app/src/components/RangeActionPicker.tsx` (NEW).** Small bordered-chrome modal that opens after range capture. Same visual register as VerseActionMenu (bg-black/40 backdrop, bottom-sheet on mobile, centered on desktop, max-w-md). Header: "Range captured — N verses" + spectral-blue range reference. Three action items: "Highlight range" (Live, ✎ icon, routes to multi-target HighlightPicker), "Copy range with watermark" (Coming-soon, ⧉, W7), "Share range with watermark" (Coming-soon, ↗, W7). Close affordances: ✕ button via Cancel footer, tap-outside, Escape — any close path exits range mode entirely per the §21 "close = cancel" model.
  - **`app/src/components/HighlightPicker.tsx` (multi-target mode).** New optional prop `targetVerseIds?: number[]`. When present and length > 1: title reads "Mark N verses"; S117 chips row hidden; S117 cap-reached banner skipped (per-verse cap enforced at commit time); save button reads "Mark N verses"; on save, fires `Promise.allSettled` over N verses with `createOrReplaceHighlight` per id; per-verse failures tolerated (the successful commits stand via individual onSaved callbacks; total-failure shows inline error and keeps picker open; partial-failure closes with a console.warn). Single-verse mode (when targetVerseIds is undefined or length ≤ 1) is unchanged — the S117 chips + cap + "Mark verse" / "Add mark" / "Already marked" labels all stay active per the existing partner mental model.
  - **`app/src/App.tsx` wiring.** New state: `rangeState: RangeSelectionState` (idle/selecting/captured), `rangePickerOpen: boolean`, `pickerRangeVerseIds: number[] | null` (drives the multi-target HighlightPicker invocation, distinct from the single-verse `pickerVerseId` so the two modes stay cleanly separable). New handlers: `buildVerseRef(verseId)` (lifts a verseId into a RangeVerseRef using chapterDetail's book + chapter context), `startRangeFromVerse(verseId)` (called from "Start range here" menu item), `commitEndVerse(verseId)` (called from verse-container click + word-tappable click when in selecting mode), `cancelRange()` (idempotent reset to idle). New useEffects: (a) chapter-navigation-cancels-range silently when selectedBookSlug or selectedChapter change (per the same-chapter UX scope); (b) Escape-key-cancels-range when selecting (RangeActionPicker has its own Escape handler when captured + picker open). Verse-render: each verse container gets `range-anchor` or `range-captured` class based on rangeState; verse-level onClick committed end-verse when status is selecting; word-tappable onClick reinterpreted as end-verse commit when status is selecting (Strong's lookup suppressed inside range mode per §21 interaction-conflict resolution). buildMenuSections gets new `onStartRange` handler + a new "Range" section with "Start range here" Live item; S122 "Multi-verse range" Coming-soon stub removed from Share section per the §20 patch.
  - **`app/src/index.css` — range-mode visual treatment.** `.verse-interactive.range-anchor` gets a 2px left-border in §5 spectral-blue + an 8% alpha accent tint + 4px left padding + -6px negative margin to make room for the border without shifting layout. `.verse-interactive.range-captured` gets the same 8% accent tint without the border. Both use `color-mix(in srgb, var(--reader-accent) 8%, transparent)` so the tint adapts automatically to the future-wheel accent re-pick (S115 deferred saturation observation).
  - **Range-mode banner in App.tsx.** Bordered-chrome panel inside the article (above the chapter title) shown when `rangeState.status === "selecting"`. Left side: "Range mode — tap an end verse" (spectral-blue "Range mode" label + muted instruction). Right side: Cancel button in the bordered-chrome family. `role="status"` + `aria-live="polite"` so screen readers announce the mode change.
  - **No schema migration, no API change.** Multi-verse highlight uses the existing `verse_highlights` table (S113 schema + S117 multi-mark constraint update) with N rows — one per verse in the range. The per-verse S117 constraint `verse_highlights_user_verse_color_style_unique` makes re-marking a verse already carrying (color, style) a silent no-op via ON CONFLICT DO NOTHING semantics. PWA-only deploy.
  - **Spec-then-build edit caught mid-build.** First draft of §21 said "Cancel from the action picker returns to range mode (range stays captured; partner can re-open the picker by tapping the captured range)." During code review I realized the "re-open by tapping the captured range" affordance needed explicit wiring + UX clarity that didn't justify V1 scope. Updated §21 to the simpler "close = cancel" model BEFORE shipping code; spec doc and code stay in sync. The lesson here: spec-then-build doesn't mean spec-is-frozen — spec-discovers-build-time-simplification is a valid spec update, and the discipline is to update the spec FIRST then ship spec + code together rather than letting code drift past spec.
  - **Verification.** `tsc -b` clean exit 0 across the entire project. 18/18 node sanity tests passing in `/Users/mtm/Desktop/App/_s123_range_selection_sanity.mjs` covering: state-machine transitions (idle → selecting → captured → idle); same-chapter forward range; range-of-one (start === end); reversed range auto-normalize; cancel from any state; commitEnd on idle / already-captured (no-op); same-chapter resolver ordered verse_ids; full-chapter range; cross-chapter range (helper captures state, same-chapter resolver returns []); cross-book range (helper captures state, same-chapter resolver returns []); cross-book reversed range normalizes by bookSlug lexicographic order (NOT canonical — documented as the W7 cross-book consumer's wrap target); out-of-order chapterVerses input handled; partial chapterVerses returns partial; isReversed flags reversed-but-unnormalized; predicates return false for null endpoints; compareRefs ordering across book/chapter/verse including the lexicographic-not-canonical bookSlug case; orderRefs symmetric in input order. Two sanity-test failures caught + fixed on first run — both test expectations had baked in canonical Genesis-then-Exodus ordering that the helper deliberately doesn't carry (per the documented lexicographic-fallback behavior). Vite build couldn't run in the sandbox (rolldown native binding architecture mismatch per the established S116/S117/S121/S122 pattern); Render's fresh Linux runtime builds cleanly. tsc-clean + sanity-tests-clean is the load-bearing verification.
  - **PWA-only deploy.** No API changes; no schema migration. Single Static Site rebuild on Render ~2min. Deploy script at `_session123_range_selection_deploy.sh` following the S120/S121/S122 chat-paste-vs-bash-script rule.

- **VerseActionMenu disabled-state stubs + layout refinement — DONE (S122, iteration on the S121 W3 ship):** Fills the contextual menu with the full S122 stub catalog so partners see the full tool surface up front. Iteration on Wheel 3, NOT a new wheel — the locked 13-wheel pre-launch sweep stays intact. Partners learn the menu shape now and items promote across render states as future wheels (W5 Notes/Bookmarks, W6 Share-with-watermark, W9/W12 reference library + recommendations, W10 interlinear, W11 nikkudot) land. Specifically:
  - **DESIGN_LANGUAGE.md §20 updated first, code second** (post-S113 forward standard held). New sub-section *Disabled-state stubs and tier-locked surfaces (locked S122)* with the three-render-state spec (Live / Tier-locked / Coming-soon), the partner-tier-aware rendering rule, the tier-badge + Coming-soon visual register, and the full locked stub catalog (16 items across 5 sections). Plus modal-sizing update — `max-w-sm` → `max-w-md`, new `max-h-[85vh] overflow-y-auto` for the fuller menu (word-scope on a Hebrew word now carries 15 items).
  - **Three render states.** **Live** = full opacity, item-specific hint (book ref for Copy, Strong's number for Strong's), invokes `onSelect`. **Tier-locked** = 40% opacity, tier-name chip badge (`Notes` or `Library` — name not price per the S117 lesson against inline-priced surfaces), invokes `onSelect` which the parent has wired to route to `/pricing`. **Coming-soon** = 40% opacity, `Coming soon` italic muted hint, no-op tap (just closes menu). Differentiated cursor (`pointer` vs `not-allowed`) reinforces the tap affordance.
  - **Partner-tier-aware rendering.** A tier-locked stub renders as Coming soon (NOT as locked) when the partner is already at or above the required tier. A free partner sees BDB as `Library`-badged + tap-to-`/pricing`; a Library-tier partner sees BDB as Coming soon because they don't need to upgrade — they're just waiting for it to ship. The /pricing route is reserved for partners who genuinely need to upgrade. Implemented via `partnerAtOrAboveTier(partnerTier, required)` helper in App.tsx's `buildMenuSections`; the component itself stays dumb rendering — `buildMenuSections` resolves item state per partner BEFORE passing items in.
  - **MenuItem interface extension (S122).** Two new optional fields: `lockedTier?: "notes" | "library"` and `comingSoon?: boolean`. The existing `disabled?: boolean` stays as the catchall no-op for any future use. `lockedTier` and `comingSoon` are the two named S122 states that drive the differentiated rendering. Future wheels promote items across states by mutating `buildMenuSections` only — when W5 ships Bookmark, the `comingSoon: true` flag on the Bookmark item gets removed and an `onSelect` handler gets wired up. Menu shape never changes; items just promote.
  - **Locked stub catalog (S122).** Word study (word-scope only): Strong's (Live) + BDB (Library, Hebrew only) + Thayer's (Library, Greek only) + Vine's expository (Library) + interlinear (Library) + Nikkudot siblings (Library, Hebrew only). Marking (verse-scope): Highlight (Live) + Bookmark (Coming soon, W5). Notes (verse-scope): Add note (Coming soon, W5, Free) + Open notes for this verse (Notes tier, W5). Cross-references (verse-scope): Treasury (Library, W9) + Nave's topical (Library, W9) + Related passages (Library, W12). Share (verse-scope): Copy verse (Live) + Share with watermark (Coming soon, W6, Free) + Multi-verse range (Coming soon, W6, Free). Total in word scope: 15 items for Hebrew, 14 for Greek. Total in verse scope: 10 items.
  - **Browser-native /pricing routing** (no react-router import needed — App.tsx's routing model is `window.location.href = ...` per the line ~119 comment). Tier-locked stubs' `onSelect` is wired in `makeTierStub()` to `window.location.href = "/pricing"`. Reuses the existing routing primitive; consistent with how other CTAs (Become-a-partner, Resubscribe) navigate elsewhere in the app.
  - **AskUserQuestion gates settled this session.** (1) Visual treatment = hybrid (tier badge for paid stubs, "Coming soon" for unshipped free stubs); two visual treatments that match the two real states. (2) Tap behavior = route tier-gated to /pricing, no-op on Coming-soon; mirrors the S113 HighlightPicker pattern for locked swatches. Both recommended paths locked.
  - **Verification.** `tsc -b` clean exit 0 across the entire project. 22/22 node sanity tests passing in `/tmp/_s122_sanity.mjs` covering: 12 tier-ladder cases (anonymous / free / study_notes / extras / complete_study / everything × notes-locked / library-locked); 6 stub-state resolutions (free→locked, extras→Coming-soon, study_notes→Library-locked-but-Notes-Coming-soon, everything→Coming-soon for both tiers); 4 language-conditional catalog cases (Hebrew word = 5 Word-study items with BDB+Vine's+Interlinear+Nikkudot; Greek word = 4 items with Thayer's+Vine's+Interlinear; verse-scope = 0 Word-study items; Greek-interlinear label correct). Vite build couldn't run in the sandbox (rolldown architecture mismatch per the established S116/S117 pattern); Render's fresh Linux runtime builds cleanly.
  - **PWA-only deploy.** No API changes; no schema migration. Single Static Site rebuild on Render ~2min. Deploy script at `_session122_menu_stubs_deploy.sh` following the S120 chat-paste-vs-bash-script rule.
  - **Drift caught + fixed mid-session.** The pre-launch sweep numbered list in this file carried a leftover duplicate `3. Strong's tap-on-word UI in PWA.` placeholder from S121 when the original-and-replacement entries were both left in place. Fixed as part of S122 per the drift-caught-while-editing-the-doc-gets-fixed-as-part-of-the-wheel forward standard.

- **Strong's tap-on-word UI + contextual VerseActionMenu architecture — DONE (S121 second wheel of the session, Wheel 3 of the locked pre-launch sweep):** Closes the §9 free-tier tap-on-word promise. Built on the S120 data foundation (8,674 Hebrew + 5,523 Greek lexicon entries; 348,884 USFX-aligned tokens) + the existing S120 endpoints. Adds a new batched chapter-words endpoint (replaces N parallel per-verse fetches for long chapters like Psalm 119 — browsers cap ~6 concurrent connections per host so N=176 serializes badly), a verse-text-↔-verse-words alignment helper, a Strong's lookup modal, and a contextual action menu that replaces the S113 direct-to-HighlightPicker long-press path with a routed-via-menu architecture that scales as Wheels 5-13 add more per-verse tools. Specifically:
  - **`app/src/lib/verse-align.ts` (NEW).** Pure helper turning `(restoredText, verseWords[])` into `Segment[]` of `plain | tappable`. Handles three patterns: (a) simple sequential matching for plain prose ("created" → H1254); (b) SINGLE sacred-name clusters ("Elohim (God)" → tappable Elohim with H0430 + plain "(God)" orientation half); (c) COMPOUND sacred-name clusters ("Yahuah Elohim (the LORD God)" → tappable Yahuah with H3068 + tappable Elohim with H0430 + plain "(the LORD God)"). The non-obvious correctness move: count how many parenthetical-internal words match upcoming USFX surfaces (call it N), then walk BACKWARDS from the parenthetical to take exactly N tokens as the Hebrew cluster. The original "claim everything between cursor and paren" version pulled prose into clusters (e.g., "that Yahuah Elohim (the LORD God)" mis-paired "that" with H3068 LORD); fixed in the second iteration before any deploy. 5/5 node sanity tests pass — Gen 1:1 simple, Gen 2:4 compound-with-prose-interlude, Gen 2:7 compound-at-sentence-start, plain-prose-with-no-cluster, empty-words-list.
  - **New API endpoint `GET /v1/books/{slug}/chapters/{n}/words`** in `api/main.py` returning `ChapterWordsResponse` (`chapter_id` + per-verse `{verse_id, verse_number, words}`). One SELECT joining verses to verse_words for the chapter; one round trip per chapter regardless of verse count. Public, no auth, no tier gate (free-tier feature per §9). The per-verse endpoint `/v1/verses/{id}/words` stays alive for single-verse needs (search result preview, share-verse hover). New `ChapterVerseWords` + `ChapterWordsResponse` models in `api/models.py`.
  - **`app/src/components/StrongsLookup.tsx` (NEW, ~165 lines).** Fixed-position modal mirroring the S113 HighlightPicker pattern (bottom-sheet on mobile, centered on desktop, bordered-chrome panel, tap-outside-to-close). Surface English word + Strong's badge (`H####` / `G####` in §5 spectral-blue accent register) + language tag. Lemma rendered at 2xl with Hebrew (rtl + SBL Hebrew font stack) or Greek (SBL Greek font stack) per language. Transliteration, pronunciation, gloss, full definition, derivation. Escape-to-close + ✕ button. Loading + error states. Mounts via `getStrongEntry(strongNumber)` from `lib/api.ts` (new client function).
  - **`app/src/components/VerseActionMenu.tsx` (NEW, ~115 lines).** Item-driven contextual menu that opens on long-press / right-click, replacing the S113 direct-to-HighlightPicker path. Items passed in via props so the caller decides what's available in the current scope; future wheels append items via App.tsx's `buildMenuItems` helper without changing the component. Word scope (long-pressed on a tappable word) shows Strong's at top + divider + verse-scoped actions below; verse scope (long-pressed on plain text) shows verse-scoped actions only. `role="menu"` + `role="menuitem"` + `role="separator"` for accessibility; 44px+ hit target per §13.
  - **`app/src/App.tsx` wiring (the big change).** New state — `menuState` (long-press menu context with optional word scope), `strongsState` (open Strong's modal target), `wordsByVerse` (chapter-words map). Refactored long-press path: `handlePointerDown(verseId, word?)` now opens the menu instead of the picker. New `handleWordQuickTap` fast path for word-level taps that bypasses the menu and opens StrongsLookup directly. New chapter-words fetch effect fires alongside the existing highlights/chapter effects. New verse-render: each verse passes `(v.text, wordsByVerse[v.id])` to `alignVerse`; renders `Segment[]` as a mix of `<span>{seg.text} </span>` for plain runs and `<span className="word-tappable" onPointer*>...` for tappable words. `e.stopPropagation()` on all word-level pointer handlers so verse-level handlers don't double-fire. The existing S121 W2 swipe + S116 reading-position + S113 mark-rendering all coexist cleanly.
  - **`app/src/index.css` — `.word-tappable` class.** Invisible at rest per DESIGN_LANGUAGE.md §1's clean reading-surface principle. Faint dotted underline in §5 spectral-blue on hover via `@media (hover: hover)` so mouse users see the affordance; touch-only surfaces discover tappability through use. `-webkit-tap-highlight-color: transparent` to suppress mobile tap-flash.
  - **DESIGN_LANGUAGE.md new §20 Tap-on-Word.** Locks the two-input-path-three-modal-surface architecture, the VerseActionMenu item-extensibility model with the Wheel 3 / Wheel 5 / Wheel 6 / Wheels 9-10 / Wheel 12 future-item roadmap, the StrongsLookup modal layout, the verse-render alignment with sacred-name cluster handling, the invisible-at-rest visual register decision, the interaction-conflict resolution with S113 / S121 W2, accessibility (44pt iOS / 48dp Android hit targets), and the deliberate V1 exclusions (always-visible tappability, hover preview, tap-and-hold gradient, per-word bookmark, cross-references from tapped word — all considered and declined for V1).
  - **Two-surface deploy (W3 is NOT PWA-only like W2 was).** API push triggers Render rebuild (~3-5min); PWA push triggers Static Site rebuild (~2min). Deploy script handles both atomically per the S120 chat-paste-vs-bash-script discipline. Brief window where the new PWA loads against the old API and the chapter-words endpoint 404s — PWA degrades gracefully to all-plain rendering until the API ships, then tap-ability lights up on next chapter load. Acceptable for a non-critical free-tier feature.
  - **Yoshi's mid-session UX call sharpened the wheel.** Original plan was direct "quick-tap → Strong's, long-press → HighlightPicker (unchanged S113)." Yoshi flagged the architectural problem — Wheels 5-13 add more per-verse tools (Notes, Bookmarks, Share, Interlinear, Recommendations) and the long-press path is going to need a contextual menu. Built the menu architecture as part of W3 so partners learn one interaction grammar across the whole tool surface, not "long-press meant X at S121 but means Y after Wheel 5 ships." One-tap regression on the highlights path (long-press → menu → "Highlight verse") accepted in exchange for the extensibility win. **Forward standard reinforced: when a wheel adds a new interaction that overlaps an existing one, build the routing architecture as part of that wheel rather than retrofitting later.**
  - **Three follow-up calls from Yoshi mid-wheel, all landed in the same ship:** (a) **Menu must group into categories**, not a flat list — refactored to MenuSection[] with 5 locked sections (Word study / Marking / Notes / Cross-references / Share); component drops empty sections; future wheels append to the right section. (b) **Copy must be a menu item** — verse copy was hard to do without fighting the long-press picker; added "Copy verse" in the Share section using `navigator.clipboard.writeText` directly (bypasses DOM selection entirely; no whitespace artifacts; pre-formatted with reference + watermark line). Verse-range Copy + Share-range deferred to Wheel 6 alongside share-with-watermark, **with explicit no-cap-ever lock — the framework's diagnostic often runs across passages the Reformation traditions truncate, the app should never reproduce that truncation in its sharing affordances.** (c) **Strong's needs to link other places the word is used (concordance)** — lexicon-card-only is "kinda weak"; partners need to see every verse where the word appears + tap to navigate. Added new `GET /v1/strongs/{number}/occurrences?limit=&offset=` endpoint (paginated; H0430 Elohim ~2600, H3068 Yahuah ~6800), new section in StrongsLookup modal with "Show more" pagination, tap-to-navigate that sets selectedBookSlug + selectedChapter + currentVerse via the W2 nav handlers' state-reset contract. Concordance failure is non-fatal — lexicon entry still renders if the endpoint fails.
  - **Render-space bug caught + fixed pre-ship.** Initial W3 verse-render emitted `<span>{seg.text}</span>` for tappable segments with NO trailing space; plain segments had a trailing space. Adjacent tappable segments (compound clusters like "Yahuah Elohim") rendered as "YahuahElohim" — no space. Tappable-then-plain ("Elohim" + "(God)") rendered as "Elohim(God)" — no space. Yoshi caught it by copying Gen 1:1 and pasting the visibly-broken output. Fix: every segment now ends with a trailing `{" "}` outside its tappable boundary. Reproduces the source verse text's word spacing exactly. **Forward standard: when emitting per-segment spans for text that was tokenized on whitespace, every segment emits a trailing space — never assume "the next segment will have a leading space."** Saves a copy-bug class.
  - **Verification.** `tsc -b` clean exit 0 across the entire project; Python `py_compile` clean on the API; 5/5 node sanity tests on the verse-align helper covering single + compound + interlude + sentence-start + plain prose + empty words. Vite build couldn't run in the sandbox (rolldown native binding architecture mismatch per the established S116/S117 pattern); Render's fresh Linux runtime builds the bundle cleanly. tsc-clean + py_compile-clean + unit-tests-clean is the load-bearing verification.

- **Chapter swipe + arrow navigation — DONE (S121, Wheel 2 of the locked pre-launch sweep):** Touch swipe (mobile/tablet/Capacitor), ← / → arrow keys (desktop/web), and visible bordered-chrome arrow buttons in both the picker row and a new bottom-of-chapter continuation row. All tiers, all surfaces. PWA-only — no schema migration, no API change, no Render rebuild. Integrates transparently with S116 reading-position save (the existing save effect already fires on `selectedBookSlug` / `selectedChapter` / `currentVerse` changes; nav handlers reset `currentVerse` to 1 mirroring the S116 picker handlers). Specifically:
  - **`app/src/lib/chapter-nav.ts` (NEW, 165 lines).** Pure helpers — `lastChapterNumber(resp)`, `booksInSameCategory(books, slug)`, `bookIndexInCategory(books, slug)`, `nextChapterTarget(books, slug, ch, resp)`, `prevChapterTarget(books, slug, ch)`, `targetLabel(books, target)`. Returns `NavTarget | null` shape (null = bounce at category edge). The prev-across-book-boundary case returns `chapter: "last"` so the caller knows to fetch the destination book's chapter list before setting state. Forward navigation across a book boundary is always `chapter: 1` so no fetch is needed.
  - **Witness-category boundary honored.** Canon → canon (66 books in canonical_order); apocrypha → apocrypha; pseudepigrapha → pseudepigrapha. Crossing from canon into apocrypha is a deliberate picker action, not a swipe. Verified by the 12-case unit test (Tobit chapter 1 prev → bounce, doesn't bleed back into Revelation 22).
  - **`app/src/App.tsx` wiring.** `prevTarget` + `nextTarget` useMemos compute the targets each render; `navigatePrev()` (async; fetches the prev book's chapters when crossing a boundary, falls back to chapter 1 of the destination if fetch fails) + `navigateNext()` (sync; forward boundary is always chapter 1). Three input surfaces converge on these two handlers — (a) window-level keydown listener for ArrowLeft / ArrowRight, skips when focus is in input/select/textarea/contenteditable, skips when any modifier key is held so browser back/forward shortcuts pass through; (b) article-level pointer handlers for touch swipe with 60px horizontal threshold + |dx| > |dy| * 1.5 angle constraint + `pointerType === "touch"` filter (mouse drag / stylus drag don't trigger nav) + S113 long-press cancellation if horizontal movement >10px so a swipe over a verse doesn't open the picker mid-swipe; (c) two pairs of bordered-chrome buttons — one in the picker row (compact, 2.5rem hit target, chevron glyph in §5 spectral-blue accent), one at the bottom of the chapter as a continuation row (larger affordance with destination preview label like "← Genesis 1" / "Genesis 3 →" so the reader who finishes a chapter doesn't have to scroll back up to the picker).
  - **Bounce affordance.** At witness-category edges (Genesis 1 prev, Revelation 22 next in canon; equivalent edges in any other category), the disabled arrow fires a 300ms shake animation (`@keyframes nav-bounce-shake` in index.css) to confirm the input was registered without changing the chapter. `prefers-reduced-motion: reduce` honored — still shows the disabled-opacity affordance but skips the shake. `aria-disabled="true"` set on the disabled button so screen readers announce the edge state.
  - **DESIGN_LANGUAGE.md updates.** §1 amended with a note that picker selects, theme toggle, subscription CTAs, and chapter-nav arrows all live in the same bordered-chrome button family. §5 swapped from gold to spectral blue (S115 drift caught + fixed; `#0084FF` is what shipped at S115, design language was still naming the original `#D4AF37` gold spec — same kind of drift fixed across three files at S120). New **§19 Chapter Navigation** locks the full spec: three input surfaces, gesture thresholds, keyboard contract, book-boundary behavior including the witness-category boundary rule, Genesis 1 = bounce / Revelation 22 = bounce decision (wrap was considered and declined to preserve the canon's creation → new creation directionality), S116 integration, accessibility (44pt iOS / 48dp Android hit targets per §13, aria-labels, tab order, modifier-key passthrough), and the deliberate exclusions (cross-edition swipe, edge-of-canon wrap, swipe-velocity prediction, hover-revealed arrows — all considered and declined for V1).
  - **Yoshi's call on Genesis 1 boundary.** Per the S121 brief, Yoshi flagged "first chapter of Genesis bounces or wraps to Revelation last chapter (my call when wheel opens)." When asked, Yoshi noted he didn't remember calling it as a question — Claude defaulted to bounce both ends (the standard pattern, preserves narrative arc) and locked it into DESIGN_LANGUAGE.md §19 with the wrap alternative documented as a one-constant change if the call ever flips.
  - **Verification:** (a) `tsc -b` clean across the entire project, exit 0 with no diagnostics — type-checks the new `chapter-nav.ts` module and the App.tsx prevTarget/nextTarget useMemos + navigatePrev/navigateNext signatures + the new bouncePrev/bounceNext state shapes + the swipeStartRef + pointer event handlers + the new `targetLabel` import. (b) 12-case node sanity test against inlined pure-function copies of the nav helpers — all pass (within-book prev/next, end-of-book forward boundary, start-of-book backward boundary, canon edges Genesis 1 prev + Revelation 22 next both bounce, apocrypha edges Tobit 1 prev bounces correctly without bleeding back into Revelation, no-chapters-resp returns null defensively). Vite build couldn't run in the sandbox (rolldown native binding architecture mismatch between Yoshi's macOS install and Linux x64 sandbox per the established S116/S117 pattern); Render's fresh Linux install builds cleanly. tsc-clean + unit-test-clean is the load-bearing verification.
  - **Two side tasks queued at S120 stay queued.** (a) watermark design (paired with Wheel 6 share-verse); (b) Genesis 1 first-chapter behavior was the call this session settled — bounce both ends. Both deferred items captured at S120; W2 closed the Genesis-1 decision, the watermark stays on the list.

- **Strong's tap-on-word data foundation — DONE (S120 Wheel 1, first wheel of the locked all-three-tiers pre-launch sweep):** Loaded the Hebrew + Greek Strong's lexicons into `strong_entries` and parsed the existing USFX-tagged KJV into `verse_words` for every Strong's-tagged English token across all 66 canon books. Two API endpoints shipped serving the §9 free-tier tap-on-word feature. Full chain verified live with three curl tests. Specifically:
  - **Two lexicon loaders shipped in `restoration-pipeline/_session120_load_strong_entries.py`.** Hebrew XML at `source-texts/strongs-hebrew/StrongHebrewG.xml` (6.2MB, OSIS format with `<div type="entry" n="N">` wrappers around `<w lemma morph POS xlit ID xml:lang>` headwords plus `<list>` of `<item>` glosses — David Troidl + Instone-Brewer 2010 cleanup pass). Greek XML at `source-texts/strongs-greek/strongsgreek.xml` (2.6MB extracted from `StrongsGreekDictionaryXML_1.4.zip`, 2007 single-file consolidation, `<entry strongs="N">` wrappers around `<greek BETA unicode translit/>` headwords plus `<strongs_def>` + `<kjv_def>` + `<strongs_derivation>` + `<pronunciation>` children). Two separate parse functions (`parse_hebrew_osis()` + `parse_greek_strongsdictionary()`) because the schemas are completely different. Both yield row dicts matching the `strong_entries` schema (strong_number / language / lemma / transliteration / pronunciation / short_definition / definition / derivation). Idempotent UPSERT by `strong_number` PK. Load counts: **8,674 Hebrew entries + 5,523 Greek entries** (matches Strong's expected ~14,200 total). Sample `H0001` = אָב / ʼâb / father; sample `G3056` = λόγος / logos / "Divine Expression (i.e. Christ)".
  - **`restoration-pipeline/_session120_load_verse_words.py`** parses the existing `source-texts/kjv/eng-kjv_usfx.xml` file, walks every `<book>` → `<c>` → `<v>` → `<ve/>` block, extracts every `<w s="H####|G####">surface</w>` element in source order, maps to verse_id via `(book_slug, chapter_number, verse_number)` lookup against the canon edition, idempotent DELETE-ALL-canon + INSERT in single transaction (USFX file IS the source-of-truth for word alignment, no drift risk from partial updates). PROTESTANT_66 USFX→slug map inlined per the existing image-shipped-loader-no-cross-file-imports pattern. Load count: **348,884 Strong's-tagged tokens** across all 66 books. Sample Genesis 1:1: 6 tagged words in position order — *beginning(H7225) God(H0430) created(H1254) heaven(H8064) and(H0853) earth(H0776)*.
  - **Two API endpoints in `api/main.py`** — `GET /v1/strongs/{strong_number}` (canonicalizes case + zero-padding, 4XX on malformed input, 404 on unknown number, full StrongEntry response on hit) and `GET /v1/verses/{verse_id}/words` (404 if verse doesn't exist, empty `words: []` array if verse exists but has no tagged tokens, full position-ordered VerseWord list on hit). Three pydantic models added (`StrongEntry`, `VerseWord`, `VerseWordsResponse`). Both endpoints public — no auth, no tier gate (free-tier feature per §9). All 8,674 + 5,523 + 348,884 rows accessible via the API.
  - **Dockerfile + `.dockerignore` updates** to ship the S120 loaders + the source-text XML files inside the image per the S112 future-loader pattern (so loaders run from Render Shell against internal Postgres, sidesteps external-Postgres SSL/IP rejection). `.dockerignore` exclusions for `source-texts/kjv/` + `source-texts/strongs-hebrew/` + `source-texts/strongs-greek/` removed; matching COPY lines added to `api/Dockerfile`. Also negate-patterns added for the two S120 Python loaders + a forward-fix backfill for the S117 multi-mark loader that was missing one.
  - **Verified live end-to-end:** all three curls returned full real data — `/v1/strongs/H0001` (Hebrew OSIS path), `/v1/strongs/G3056` (Greek 2007 path), `/v1/verses/192132/words` (Genesis 1:1 — the verse_id was discovered by querying the existing `/v1/books/genesis/chapters/1` endpoint since BIGSERIAL IDs in the seed don't start at 1). The free-tier tap-on-word foundation is shipped. The data sits underneath the entire $4.99 reference library wheel (BDB / Thayer's / Vine's / ATS / Easton's / Smith's / Nave's / Treasury) + Hebrew/Greek interlinear + nikkudot-strip + verse-highlight recommendations engine — Wheels 3-12 all get easier from here because Strong's is the join column.

  **Pre-launch sweep sequence locked S120, re-sequenced S122 (14 wheels total to ship before Google Play listing — multi-verse highlight added to scope and range-selection architecture inserted ahead of W5):** Per Yoshi's all-three-tiers-complete-before-listing call (preserves the §16 strategic-frame principle that every listed-in-store feature must actually work because there's no existing audience to forgive a thin V1):
  1. ~~Strong's data + API foundation~~ — **SHIPPED S120**.
  2. ~~Chapter swipe + arrow navigation~~ — **SHIPPED S121** (touch swipe + ← / → keyboard + visible chrome arrows + bottom-of-chapter continuation row; bounce at witness-category edges; PWA-only deploy; integrates transparently with S116 reading-position save).
  3. ~~Strong's tap-on-word UI in PWA~~ — **SHIPPED S121 (second wheel of the session); MENU SURFACE ITERATED S122** (verse-render alignment via new lib/verse-align.ts handling single + compound sacred-name clusters; batched chapter-words API endpoint replacing N parallel per-verse fetches; VerseActionMenu contextual-menu architecture that scales for Wheels 5-13's tools; StrongsLookup modal; quick-tap-word fast path for the high-frequency Strong's lookup; long-press / right-click routes through the menu. S122 filled the menu with the full stub catalog — every future-wheel item renders as a dimmed stub today with two states: tier-locked stubs route to /pricing for below-tier partners, Coming-soon stubs are no-op. Partner-tier-aware: a tier-locked stub renders as Coming soon when the partner is at-or-above the required tier. PWA-only deploy.)
  4. ~~Range-selection architecture + multi-verse highlight~~ — **SHIPPED S123** (shared mechanic in `lib/range-selection.ts` as a pure state machine + helpers; W4 UX is same-chapter only with helper fully general for W7 cross-chapter inheritance; "Start range here" Live in new Range section of VerseActionMenu; RangeActionPicker post-capture modal with Highlight Live + Copy/Share Coming-soon for W7; HighlightPicker multi-target mode via optional `targetVerseIds` prop firing parallel POSTs through existing `verse_highlights` schema; range-mode banner with anchor-verse 2px spectral-blue accent + 8% alpha tint + captured-range tint; DESIGN_LANGUAGE.md §21 locked at wheel-open + §20 patches for the obsolete Share-section stub; 18/18 sanity tests + tsc-b clean; PWA-only deploy).
  5. **NEW (added S122) — Bookmarks + Notes V1 (S124 scope lock).** Was W5 in the original sweep; pulled forward (W4 Search shifts to W6). Two distinct surfaces per the S122 architecture lock: **bookmarks** = single-verse flag with optional `short_description` (varchar) text field — partner can label the bookmark with a tag/title like "memorize this" or "Lo-Ammi thread anchor" (the short-description-on-bookmark is unusual relative to typical Bible apps; Yoshi's design call to make bookmarks navigable not just flag-able). **Notes V1** = §9 Free-tier single global notepad with verse-anchor support (tap verse → Add note → notepad opens with verse reference auto-inserted at cursor). A verse can carry BOTH a bookmark and a note — distinct surfaces, distinct schemas, distinct intents (bookmark = "find this again, here's why"; note = study content). Daily Scriptures stays in W5 scope per the original sweep. Notes tier ($1.99) per-verse separate notes + hub stays at the original W7 (now W8) scope.
  6. Search V1 across canon + extras (was W4 — server-side already shipped at `/v1/verses/search`; UI not built).
  7. Share-verse-with-watermark (was W6 — paired with the watermark design wheel — currently queued, task #2; watermark text derives from the locked "Remnant of Promise Official Study Bible" product name). Multi-verse Copy + range Share both ride on the S123 range-selection helper.
  8. Notes center + bookmark-by-color study view (was W7 — $1.99).
  9. Marks checklist tool — color × style × color-regardless-of-style filter surface (was W8 — $1.99).
  10. Public-domain reference library ingestion: BDB, Thayer's, Vine's, ATS, Easton's, Smith's, Nave's, Treasury (was W9 — $4.99 — heaviest data wheel).
  11. Hebrew/Greek interlinear (was W10 — $4.99 — alignment work on top of Strong's).
  12. Nikkudot-strip flagship feature (was W11 — $4.99 — consonantal-form indexing derived from BDB; S17 lock).
  13. Verse-highlight recommendations engine (was W12 — $4.99 — curated cross-reference thread surface wiring, 100% AI-free).
  14. Notes export to PDF (was W13 — $4.99).

  **Re-sequence rationale (S122 verbal-answers-transcribe).** Yoshi surfaced mid-S122 (post-menu-deploy, looking at the live stub catalog) that (a) multi-verse highlight wasn't anywhere in the sweep and should be, (b) Bookmarks + Notes V1 (the W5 work) should land before W4 Search since they're more partner-facing reading-surface tools. The shared range-selection insight (one mechanic, three consumers — multi-verse highlight, multi-verse Copy, range Share-with-watermark) pulled multi-verse highlight onto its own wheel (S123) as the architectural-first consumer rather than bundling it into W5. Bookmarks + Notes V1 takes S124 as a clean pair-wheel. Search V1 shifts from W4 to W6; everything from W6 onwards shifts one wheel later. Total sweep grows from 13 wheels to 14. The §16 strategic-frame principle still holds — every listed feature must work because there's no existing audience to forgive a thin V1.

  **Product name lock confirmed S120 + drift fixed.** `BIBLE_APP_ROADMAP.md` line 420 carries Yoshi's lock: **The Remnant of Promise Official Study Bible** (long form for landing-page H1 + store listing title) / **Remnant of Promise Official Study Bible** (short form / wordmark / watermark text). The "of-the-Remnant-of-Promise" construction was raised in earlier sessions and declined for triumphalist-institutional read (the remnant is the found, not the elite). Drift caught + fixed across three files at S120: `S118_READ_THE_SCRIPTURES_PAGE_SPEC.md` (was using `R.O.P. Official Study Bible` initialism), `DESIGN_LANGUAGE.md` §1 + §9 (was dropping "Official"), `REBUILD_STATE.md` S118 entry (same initialism drift). All now read the locked long form.

- **Outside-Matthew audit list (still queued):** `1-corinthians-11.md` body rewrite + canon.json string rewrite; `john-1.md` final-pass + short-form layer.

## Next wheels — surfaced in S112-S113 conversation, not yet built

These are the wheels Yoshi named or implied during S112-S116 that did NOT land in those sessions. Each is its own session-sized piece of work.

- **~~WordPress login URL is broken — `/login` 404s (NEW S113)~~ — SHIPPED S119.** Closed via `/goshen/` direct route (the spam-protection slug-move Yoshi had already done in WP admin months ago). `Pricing.tsx` line 153-155 now points anonymous clickers at `https://remnantofpromise.org/goshen/?redirect_to=...` instead of the dead `/login`. Full SSO bridge verified end-to-end live (incognito → /pricing → /goshen/ sign-in → bounce-back → Reactivate → Stripe Checkout). See the S119 Done block above for the full ship.
- **Missing create-account path for new partners (NEW S113) — STILL QUEUED post-S119.** S119 closed the existing-partner sign-in path via /goshen/ direct route, but /goshen/ is login-only. A first-time visitor with no WP account still has no surfaced way to register. The S118 spec docs on disk (`S118_LOGIN_REGISTER_SURFACE_SPEC.md` for the combined /account surface; `S118_READ_THE_SCRIPTURES_PAGE_SPEC.md` for the marketing landing page) carry the build design for when this becomes scope. Email deliverability fix (Login spec Part 4 — WP Mail SMTP plugin + Google Workspace SMTP or SendGrid free tier; bar <60s test arrival) is a hard prereq for the create-account wheel because email verification is part of the flow. **Defer trigger:** wheel becomes urgent when traffic starts arriving — anonymous-but-not-yet-a-partner readers landing on the PWA. Per the §16 strategic frame, that traffic doesn't exist yet, so the bleed isn't real until acquisition turns on. **Path when ready:** follow the S118 specs as written; the /account combined-surface build + email-deliverability fix + the /read-the-scriptures landing page can ship together as one wheel, or split across two sessions.
- **~~Subscription-state sync drift between our DB and WordPress user-meta~~ — SHIPPED S114 (DB-wins-over-JWT in auth.py). See the Done section above. Reconciliation script blocked by Cloudflare bot mode — see the Cloudflare wheel below.**

- **Speech-mode bracket markers — emerald (and royal purple at V2) for divine speech (DEFERRED S114).** DESIGN_LANGUAGE.md §4 locks the SVG S-curve bracket markers with the chained-quadratic path (top: `M 14 2 Q 16 6 10 9 Q 4 12 5.5 18`, bottom: `M 5.5 0 Q 4 6 10 9 Q 16 12 14 16`), stroke 4 round linecap, metallic gradient. **V1 ships emerald only for ALL divine speech** — Yahuah's OT speech and Yahusha's NT speech both carry the same emerald marker per the framework's nature-of-God reading (same Voice). V2 adds the royal purple warning marker after an editorial tagging pass classifies speech runs into promise/warning/mixed. **The brackets are NOT yet implemented on the live PWA** — Yoshi surfaced the gap mid-S114 when discussing the chrome work; deferred because the bracket renderer needs *data*: which verse ranges carry divine speech (Yahuah / Yahusha direct discourse). The data piece is the unknown — could be heuristic (anything inside red-letter-style markup on the source), schema-tagged via an editorial pass on the canon, or interim heuristic on the gospels + known prophet-speech blocks (Ezekiel address-form "son of Adam" runs, the "thus saith Yahuah" introducer in the prophets, etc.). This wheel opens with a data-plan conversation BEFORE any rendering code.

- **Cloudflare bot-mode blocking API → WP companion-plugin POSTs (NEW S114).** Surfaced when the S114 reconciliation script attempted to run from Render Shell — the Render → WP POST to `/rop/v1/partner-tier` got back HTTP 403 with a Cloudflare JS challenge page body (`__CF$cv$params` snippet in response — Cloudflare's "verify you're human" challenge). `httpx` doesn't run JavaScript, so the challenge fails and the request 403s. **Cloudflare is in the path for the WP companion-plugin endpoint and is now challenging Render's outbound POSTs.** This blocks the reconciliation script AND will block any future webhook-driven tier sync from Render. The earlier manual `customer.subscription.updated` webhook resend Yoshi did from Stripe dashboard DID work (S113), suggesting either intermittent challenge, recent Cloudflare config change, or Stripe's webhook source IP getting different treatment than Render's. **Fix paths to consider:** (a) Cloudflare WAF rule allowing requests with the `X-ROP-Signature` header (the companion-plugin authentication marker); (b) IP-allowlist Render's outbound range in Cloudflare (Render publishes the egress IPs; need static-IP feature for stable allowlist target); (c) move `/rop/v1/partner-tier` path to a Cloudflare skip-bot-mode rule; (d) Cloudflare Authenticated Origin Pulls so traffic going through CF carries verified provenance; (e) move the WP companion-plugin endpoint to a subdomain not on Cloudflare. Lowest-friction probably (a) or (c). **Workaround for Yoshi specifically (right now):** Open WP admin → Users → Yoshi → set `rop_partner_tier` user-meta to `everything` manually. Bypasses Cloudflare entirely. NOT strictly needed for the picker to work post-S114 (auth.py DB-wins means API ignores the WP user-meta value), but cleans up the WP-side UI display.
- **App Store + Google Play wraps (Capacitor).** Wrap the PWA in a thin native iOS/Android shell so the app gets storefront listings. $99/year Apple Developer + $25 one-time Google Play (Yoshi pawned to cover both). Single codebase, no rewrite. Schema + PWA already support everything; this is packaging + submission work.
- **Install buttons on the marketing surface.** Two buttons: "Get the app" (mobile/tablet → fans out to App Store + Play Store badges by device) and "Download for desktop" (triggers the PWA install prompt for Mac/Windows/Linux). No ambiguity about path.
- **Offline-first service worker + "Download everything" tap.** PWAs do offline natively via service workers. Standard pattern: app shell cached on first visit, verses + commentary + cross-references cached lazily as the partner reads, one-tap "Download everything for offline use" pre-caches the full canon + whatever extras tier they've unlocked. User data (highlights, notes, bookmarks, reading position) writes locally first, syncs when network returns. **Yoshi's commitment to all tiers including free per S47 lock — do not gate offline behind paid.**
- **Notes / bookmarks / memorization / export UI** (highlights shipped at S113; multi-verse highlight + shared range-selection mechanic shipped at S123 — see the DONE blocks above). Schema tables exist (`study_notes`, `reading_positions`); `bookmarks` table NEW for S124. Notes/bookmarks UI not built. **Tier split per Yoshi's S112 correction + the DESIGN_LANGUAGE.md §9 spec + S122 bookmark-vs-notes-distinct-surfaces lock:**
  - **Free:** single global notepad with verse-anchor support (tap verse → Add note → opens the one notepad with verse reference auto-inserted at cursor); bookmarks with optional short_description field (favorite a verse without coloring it, optionally tag/label the bookmark with a short text description — bookmark is the "find this again" surface, notes is the study-content surface; a verse can carry BOTH); reading history (last position, recently read); share / send verse with Remnant of Promise watermark (viral acquisition channel); ~~multi-verse highlight~~ (SHIPPED S123 atop the shared range-selection helper).
  - **Notes ($1.99):** separate notes per book/chapter/verse — distinct, named, scoped notes; note central hub showing all notes organized by color category + book/chapter + filter + search; bookmark-by-color topical study view.
  - **Library ($4.99):** Notes export to PDF (full notes content, formatted, downloadable); verse-highlight recommendations engine (when partner highlights a verse, app surfaces related passages from Yoshi's curated thematic cross-reference threads — 100% curated, AI-free).
- **~~Strong's tap-on-word lookup data foundation~~ — DATA + API SHIPPED S120 (Wheel 1 of the locked sweep).** `strong_entries` populated with 8,674 Hebrew (OSIS parse) + 5,523 Greek (2007 strongsdictionary parse) entries; `verse_words` populated with 348,884 tagged tokens across all 66 canon books. Two API endpoints live: `/v1/strongs/{number}` + `/v1/verses/{id}/words`. **Still queued — Wheel 3 (PWA tap-on-word UI): the verse render swap that splits rendered prose into tappable word spans backed by verse_words, plus the modal that opens a Strong's lookup when a word is tapped.** The data foundation now sits underneath the entire $4.99 reference library (Wheel 9) + Hebrew/Greek interlinear (Wheel 10) + nikkudot-strip (Wheel 11) + verse-highlight recommendations engine (Wheel 12) — those wheels all use Strong's as the join column.
- **BDB / Thayer's / Gesenius / Easton's / Smith's / ISBE dictionaries + maps + atlases + concordances** as $4.99 Library tier reference apparatus. Public-domain data + UI surfaces not yet built.
- **Statement of Faith doorway (I-VII)** surface in PWA. Schema table (`sof_sections`) exists; surface not built.
- **Daily reading plan** (free tier). Schema implied; surface not built.
- **Sacred-names display-preference toggle** (Q15 back-burner from S35).
- **ROP → Bible app menu link in WordPress.** Yoshi to add via WP admin → Appearance → Menus → custom link to `https://bible.remnantofpromise.org` with label "Study Bible." (Bible app → ROP link landed in PWA footer at S112.)
- **Screenshots for site advertising + Play Store + App Store listings.** Capture after the highlight/notes UI lands so the screenshots show the real reading surface, not the bare reader.

### Marketing-surface copy (locked S112 from Yoshi's voice)

> *"For a better experience download the app — whatever your tier is, the app is a better experience whether desktop, tablet, or mobile, and all your bookmarks, notes, and tools save across devices and platforms with your account."*

Use this verbatim on the install-buttons surface when that wheel lands.

## Per-chapter operational pattern

Existing `matthew-N.md` (database-source, already in place) is read at session-open for substance-architecture; per-chapter session work is `matthew-N-short.md` + `matthew-N-free.md` only. One chapter per session typical at NEW-band density.

## Pre-checks at-draft-time (priority-ordered)

1. **Hyphenated-compound parenthetical-on-every-mention** across ALL sub-patterns — personal-name, geographic-sacred-name, deity-name (single + compound), covenant-people-name. **Any Tanakh-substantive containing a deity-name renders with spaces; deity-name standalone with parenthetical; no compound-extension before or after parenthetical.** (Day of Yahuah, fear of Yahuah, Lamb of Elohim, Son of Elohim, goat for Yahuah, living Elohim, yielding of the Ruach HaKodesh, etc.)
2. **NO internal-document references** — unfold substance descriptively.
3. **NO framework reflexives** — use *the substance reads X / the canon walks X / the King-of-Yashar'el (Israel) walks X / the prophet-X walks X / the apostle-X walks X*.
4. **English book-names at ALL citation AND naming positions** including in hyphenated-substance-pointer-modifier-constructions. Personal-name *Yochanan (John)* OK in prose; book-citation is *John X:Y*.
5. **Son-of-Adam absolute with kaph-comparative narrow-carve-out at Daniel 7:13 / Revelation 1:13 / Revelation 14:14 ONLY.**

Short.md sweep-corrections do NOT automatically internalize for free.md drafting — re-trigger discipline-attention explicitly at-draft-time on free.md.

## Word-count calibration

| Chapter | Short.md | Free.md | Band |
|---|---|---|---|
| matt-13 | 7,517 | — | framework-density-peak |
| matt-14 | 4,335 | — | narrative |
| matt-15 | 4,062 | — | narrative |
| matt-16 | 8,731 | — | multi-major |
| matt-17 | 8,482 | — | multi-major |
| matt-18 | 10,299 | — | multi-major-PLUS |
| matt-19 | 9,794 | — | multi-major |
| matt-20 | 10,614 | — | multi-major-PLUS |
| matt-21 | 10,724 | — | multi-major-PLUS |
| matt-22 | 12,377 | 3,676 (3.37) | NEW |
| matt-23 | 10,803 | — | multi-major-PLUS |
| matt-24 | 16,331 | 3,247 (5.03) | SECOND-NEW |
| matt-25 | 14,237 | 2,872 (4.96) | BETWEEN |
| matt-26 | 12,932 | 2,868 (4.51) | NEW |
| matt-27 | 13,896 | 2,009 (6.92) | BETWEEN |
| matt-28 | 12,151 | 3,326 (3.65) | NEW |

**Bands**:
- Narrative: 4,000-4,500
- Framework-density-peak: 6,000-7,500
- Multi-framework-major: 8,000-9,800
- Multi-framework-major-PLUS: 10,000-11,500
- NEW (one FRAMEWORK-MAJOR + Tanakh-cluster): 12,000-13,500
- BETWEEN-NEW-and-SECOND-NEW: 13,500-15,000
- SECOND-NEW-BAND-ABOVE: 15,000-17,000+

Free.md compression-ratio historical range: 3.37-6.92. Honest-compression preferred over ratio-alignment.

## Wrap-signal triggers (S93 calibration)

1. Substantive-turn count ≥25-30
2. Context budget ≥~70%
3. Observable-error-rate clustering (multiple sweep-catches in close succession on similar work)

Fatigue-feeling is NOT a wrap signal. When wrapping, transcribe verbal answers into source-of-truth files BEFORE closing — answers that live only in conversation get lost at session boundary.

## Recent observations (keep last 1-2 sessions only; trim older)

- **S123 (Range-selection architecture + multi-verse highlight — Wheel 4 of the locked 14-wheel pre-launch sweep):** Clean ship of the shared mechanic + first consumer. Spec-then-build held throughout — DESIGN_LANGUAGE.md §21 written FIRST with the full mechanic spec (one shared menu entry, one capture flow, one branching action picker), then the helper + components + wiring. tsc -b clean + 18/18 node sanity tests on the helper covering boundary cases (same-chapter / cross-chapter / cross-book / range-of-one / range-reversed / state-machine transitions / partial chapterVerses / out-of-order input). PWA-only deploy.
  - **One AskUserQuestion gate settled the entry shape.** Three real options presented (action-specific menu items / shared "Start range here" + action picker / gesture-only). Yoshi's answer — *"why wouldnt we have the same shared click, unless we can explain to users the different gestures"* — locked the shared-click model. One shared menu entry, one capture flow, three branching destinations in the action picker (Highlight Live this wheel, Copy + Share Coming-soon for W7). When W7 ships, only the action picker changes — entry path stays stable. **Forward standard re-confirmed: when a wheel introduces a mechanic that multiple future consumers will share, default to one shared entry-point unless there's a clear story for why differentiated entries help partners.**
  - **Architecture-now-beats-retrofit-later, third instance.** S121 W3 menu (one routing mechanic, 5+ future consumers across Wheels 5-13); S122 partner-tier resolution (one helper, every future tier-gated surface); S123 range-selection (one mechanic, 3 known consumers — multi-verse highlight ships now; multi-verse Copy + range Share-with-watermark consume the same captured state at W7). Each instance reinforces: the architectural piece IS the wheel; the first consumer is the verification surface. Bundling all consumers into one big multi-feature wheel costs more than building the shared infra once and adding consumers one wheel at a time.
  - **Spec-then-build with mid-build spec amendment.** First draft of §21 said "Cancel from the action picker returns to range mode (range stays captured)." During code review I realized this needed an explicit re-engage affordance that didn't justify V1 scope. Updated §21 to the simpler "close = cancel" model BEFORE shipping code, so spec and code stay in sync. **Forward standard sharpened: spec-then-build doesn't mean spec-is-frozen — when build-time reveals a simpler design, update the spec FIRST then ship spec + code together. Letting code drift past spec is the failure mode this guards against.** First instance of mid-build spec amendment in the sweep.
  - **Pure-helpers + node sanity tests, fourth wheel running.** Same pattern as S121 W2 chapter-nav (12 cases), S121 W3 verse-align (5 cases), S122 partner-tier resolution (22 cases). S123 range-selection: 18 cases inlined into `_s123_range_selection_sanity.mjs`. Two failures caught on first run — both test expectations had baked in canonical Genesis-then-Exodus ordering that the helper deliberately doesn't carry (per its JSDoc'd lexicographic-fallback behavior for cross-book ordering, with W7 expected to wrap with a witness_category canonical_order-aware comparator per §19). Tests fixed + an additional assertion added explicitly confirming the lexicographic ≠ canonical gap. **Forward standard: when documenting a known gap in a helper (like "this falls back to X instead of canonical Y"), add an explicit sanity test that asserts the gap is present + names it — surfaces drift if a future change accidentally fixes it without documenting why.**
  - **Component-state isolation paid off.** The new `pickerRangeVerseIds` state was added alongside the existing `pickerVerseId` instead of unifying them, even though unification looked cleaner architecturally. The two modes (single-verse via menu, multi-target via range action picker) have different ON CLOSE semantics (single-verse close = just close; multi-target close = also exit range mode), different default chip behavior, and different success callbacks. Keeping them as separate state branches in App.tsx means a regression in one doesn't risk the other; the HighlightPicker component's `targetVerseIds?: number[]` flips internal mode without changing single-verse behavior at all. **Forward standard: when extending a component to support a new mode, lean toward "new optional prop that flips internal mode" over "refactor everything into a unified API"; the regression risk on a working single-purpose component is real.**
  - **Schema-not-needed verification.** Multi-verse highlight uses the existing `verse_highlights` table (S113 + S117 constraints unchanged) with N rows — one (user_id, verse_id, color, style) row per verse in the range. Per-verse S117 cap + per-verse insert-or-no-op semantics apply uniformly; no migration. **Forward standard: when a wheel COULD use a schema migration but doesn't have to (existing tables + constraints can carry the new behavior), prefer no-migration. Removes a deploy ordering dependency (the S117 "deploy API → run migration → small 500-window" sequence) and keeps the schema lean for actually-needed changes.**

- **S122 (VerseActionMenu disabled-state stubs + layout refinement — iteration on S121 W3, not a new wheel):** Short, tight session. Wheel-3 menu surface filled out per Yoshi's "menu surface fuller before future wheels land" call at session open. Two AskUserQuestion gates settled the visual treatment + tap behavior; spec-then-build held; tsc + 22 sanity tests clean; PWA-only deploy. Locked 13-wheel sweep stays intact — W3 is shipped and now iterated, next wheel up the sequence remains W4 (Search V1 across canon + extras).
  - **Spec-then-build pattern held cleanly.** DESIGN_LANGUAGE.md §20 updated FIRST with the new sub-section (locked stub catalog table + three-render-state spec + partner-tier-aware rule + modal-sizing notes), THEN the code changes (VerseActionMenu.tsx + App.tsx's `buildMenuSections`). No drift between spec and shipped code. Forward standard reinforced: when iterating an existing locked section of DESIGN_LANGUAGE.md, the new sub-section gets ADDED with its own *(locked S122)* timestamp rather than rewriting the S121 lock — keeps the version history of decisions legible inside the spec doc itself.
  - **Drift-caught-while-editing held.** Found a leftover duplicate `3. Strong's tap-on-word UI in PWA.` placeholder in the Pre-launch sweep numbered list (line 145) — when the S121 ship updated the strikethrough version on line 144, the original placeholder was never deleted. Fixed as part of S122 per the forward standard from S88 / S120 / S121: when touching a section of a source-of-truth doc, also reconcile any drift between the section and reality. This is now the fourth drift-caught-while-editing-the-doc instance (S88 john-1 names, S120 product-name-three-files, S121 §5 gold→blue, S122 duplicate Wheel-3 placeholder).
  - **Pure helpers + unit-test verification on logic-heavy iterations.** The `partnerAtOrAboveTier` + `makeTierStub` + language-conditional Word-study catalog logic was extracted as pure functions in App.tsx's `buildMenuSections` helper. 22 boundary cases inlined into a node sanity script (12 tier-ladder × 6 stub-state-resolution × 4 language-conditional) caught all edge cases before deploy. Same standard as S121 W2's 12-case chapter-nav sanity + S121 W3's 5-case verse-align sanity. **Forward standard re-confirmed: when a wheel introduces non-trivial business logic, write the helpers as pure functions and exercise the boundary cases with a node sanity test before shipping.**
  - **Component stays dumb, parent owns state resolution.** VerseActionMenu got two new optional MenuItem fields (`lockedTier`, `comingSoon`) but the render logic just reads them and switches. The actual stub-state-vs-partner-tier resolution happens in App.tsx's `buildMenuSections` BEFORE items reach the component. This keeps the component testable in isolation (pass it items, see what renders) and keeps the tier logic colocated with the rest of the partner state. **Forward standard: when extending a component to support new visual states, push the state resolution UP to the parent (which already has access to user state) rather than threading user state DOWN into the component.** Saves later session debug when partner-state changes flow through.
  - **Browser-native /pricing routing kept.** App.tsx's routing model is `window.location.href = ...` (per line ~119 comment) — no react-router import. The tier-locked stubs route to /pricing via this same primitive. Reuses what's already in place; consistent with how Become-a-partner + Resubscribe buttons navigate elsewhere. **Forward standard: when a new affordance needs to navigate, use whatever routing primitive the app already uses; don't introduce a new routing dependency for a one-off.**
  - **Partner-tier-aware stub rendering is the honest UX.** Showing BDB as `Library`-locked to a Library-tier partner would be wrong — they don't need to upgrade, they're just waiting for it to ship. The flip from locked → Coming soon based on partner tier is implemented in 4 lines (`partnerAtOrAboveTier` check + branching in `makeTierStub`) and keeps the /pricing route reserved for genuine upgrade affordances. This is the same partner-trust pattern that runs through the rest of the app (S114 DB-wins-over-JWT, S115 chrome ternary across all 8 subscription states, S119 simple-is-better discipline) — show the partner the truth about their state, don't manufacture upgrade pressure on partners who already paid.
  - **Post-deploy scope-add + sweep re-sequence (verbal-answers-transcribe).** Looking at the live menu post-deploy, Yoshi surfaced three asks: (a) multi-verse highlight isn't in the sweep and should be; (b) Bookmarks + Notes V1 (W5) should land before W4 Search since they're more partner-facing reading-surface tools; (c) bookmarks should carry a short_description field — distinct surface from notes (a verse can carry both). Two AskUserQuestion gates settled the scoping: §9 holds (bookmark/notes distinct, single-global-notepad-Free stays); S123 = range-selection architecture + multi-verse highlight (first consumer of the shared mechanic, same architecture-now-beats-retrofit-later reasoning as the S121 W3 menu call); S124 = Bookmarks (with short_description) + Notes V1 (was W5). Search V1 shifts from W4 to W6; all subsequent wheels shift one wheel later; total sweep grows from 13 to 14. The shared-range-selection insight was the architectural lever — three consumers (multi-verse highlight, multi-verse Copy, range Share-with-watermark) all need the same mechanic, so building it as its own architectural piece + multi-verse-highlight-as-first-consumer is cleaner than bundling into a big multi-feature wheel. Pre-launch sweep numbered list + Next-wheels Notes/Bookmarks section both updated in this file.
  - **Architecture-now-beats-retrofit-later reinforced (third instance).** S121 W3 menu (one routing mechanic, 5+ future consumers across Wheels 5-13); S122 partner-tier resolution (one helper, applies to every future tier-gated surface, not just the menu); S123 range-selection (one mechanic, 3 known consumers). **Forward standard re-confirmed: when a wheel surfaces a new mechanic that 2+ future wheels will need, build the shared mechanic as its own architectural piece + ship the first consumer atop it, rather than bundling all consumers into one big multi-feature wheel.** The architectural piece is the wheel; the first consumer is the verification surface.

- **S121 (TWO wheels shipped — W2 chapter nav + W3 Strong's tap-on-word UI; plus a 90-day soft-launch reframe parked, summarized).** Older detail trimmed per the keep-last-1-2 convention. S121 shipped Wheel 2 (touch swipe + ← / → arrow keys + visible bordered-chrome arrow buttons + bounce-at-witness-category-edge; `lib/chapter-nav.ts` + 12-case sanity tests; DESIGN_LANGUAGE.md §19) AND Wheel 3 (Strong's tap-on-word UI built on the S120 data foundation; new batched chapter-words API endpoint + `lib/verse-align.ts` + StrongsLookup modal + VerseActionMenu contextual menu architecture; DESIGN_LANGUAGE.md §20). Also surfaced a 90-day website soft-launch reframe (parked) and a no-card 3-day promo-trial idea (future wheel when acquisition becomes scope). Disciplines that carry forward: **spec-then-build held cleanly** (DESIGN_LANGUAGE.md updates BEFORE code); **pure-helpers + unit-test verification on logic-heavy wheels** (12-case chapter-nav + 5-case verse-align); **bordered-chrome button family locked for navigation chrome**; **forward-flow nav handlers reset currentVerse=1 mirroring S116 picker handlers**; **architecture-now beats retrofit-later when a wheel adds a new interaction overlapping an existing one** (W3 menu architecture call — first instance of the standard, S122 partner-tier was the second, S123 range-selection is the third); **preview-the-data-before-writing-the-parser caught real bugs pre-deploy** (W3 verse-align cluster mis-pairing on Gen 2:4); **two-surface API+PWA deploy needs deploy-order awareness** (API first, PWA second, graceful-degradation gap acceptable for non-critical free-tier surfaces). See prior REBUILD_STATE versions in git history for full S121 detail.

- **S120 (Wheel 1 of the locked 13-wheel pre-launch sweep — Strong's tap-on-word data foundation; pre-launch sweep sequence + product-name lock settled; summarized).** Older detail trimmed per the keep-last-1-2 convention. S120 shipped the Hebrew (OSIS) + Greek (2007 strongsdictionary) lexicon loaders + USFX-tagged verse-word alignment loader + two API endpoints + Dockerfile plumbing; three curl tests verified live; 13-wheel sweep sequence + "The Remnant of Promise Official Study Bible" product name lock settled via two AskUserQuestion gates. Disciplines that carry forward: **chat-paste-vs-bash-script rule** (any deploy sequence with quotes / backslash continuations / >3 lines ships as a `.sh` script Yoshi bash-invokes; the S120 + S121 + S122 deploy scripts are the template); **data-source schema verification before parser design** (one focused `head -c 600` or browser preview of an actual record BEFORE writing the parser); **GitHub default-branch + repo-structure assumptions are time-sensitive** (verify branch + path via browser before scripting downloads); **Edit-tool stale-trailing-content trap** (after substantial Edit on a script/config file, do a full Read of the final state to catch stale trailers); **BIGSERIAL ≠ 1-based** (fetch real IDs from existing endpoints, don't guess); **verbal-answers-transcribe at session close**. See prior REBUILD_STATE versions in git history for full S120 detail.

- **S119 (anonymous-checkout /login 404 → /goshen/ direct route, summarized).** Older detail trimmed per the keep-last-1-2 convention. S119 closed the S113-surfaced /login 404 with a one-line Pricing.tsx edit pointing at Yoshi's existing /goshen/ login slug instead of building the S118-proposed combined /account surface from scratch; full incognito → /pricing → /goshen/ sign-in → bounce-back → Reactivate → Stripe Checkout verified end-to-end live (the first real verification of the full acquisition-to-subscription pipeline since S30). Disciplines that carry forward: **inherited-issue re-verification before forward build** (a session that inherits an issue from a prior session's diagnosis re-verifies the underlying state with a live test FIRST before specifying greenfield fix-path architecture — thirty seconds of "open incognito and click the broken button" would have saved the S118 spec-design work); **cross-system gate before greenfield spec** (for any cross-system wheel, gate on "what's already in place on the other side" before specifying greenfield architecture — the partner may have already solved it); **simple-is-better when the partner names a simple direct path** (when the partner names a simple direct path that closes the bleed, take it, don't reason about the more complicated path the original spec proposed); **end-to-end verification across deferred-prior-work surfaces lands at one ship** (when shipping a fix that touches a path multiple prior sessions worked on, walk the ENTIRE path end-to-end). See prior REBUILD_STATE versions in git history for full S119 detail.

- **S118 (WP /login + /account + Read-the-Scriptures landing page — specs shipped, /login half closed by S119, summarized).** Older detail trimmed per the keep-last-1-2 convention. S118 shipped two WP-admin async spec documents (`S118_LOGIN_REGISTER_SURFACE_SPEC.md` + `S118_READ_THE_SCRIPTURES_PAGE_SPEC.md`) for Yoshi's WP-admin work; the /login-404 half closed at S119 via the /goshen/ direct route; the create-account-for-new-partners half + email-deliverability fix + marketing landing page stay queued as future wheels. Disciplines that carry forward: **scope-expansion gate-question-then-spec-update pattern** (mid-wheel scope adds settle via focused AskUserQuestion before any spec changes); **marketing-surface voice rule** (back-cover/marketing copy uses conventional English per the voice skill's back-matter rule; restored sacred names render inside app body text only); **spec-doc pattern for cross-system wheels** (when a wheel requires both my side + a domain-admin side, ship build specs for the domain admin + stage my side; let admin work happen async); **email deliverability as hard-prerequisite** (any auth-surface wheel requiring email verification needs deliverability verified BEFORE the surface ships — <60s test-arrival bar); **verbal-answers-transcribe at session close**. See prior REBUILD_STATE versions in git history for full S118 detail.

- **S117 (Tailwind chrome-register sweep + multi-mark highlights + UX touch-ups + forward-fix follow-up, summarized).** Older detail trimmed per the keep-last-1-2 convention. S117 shipped two wheels in one session: (1) Tailwind chrome-register sweep — body-prose chrome surfaces propagated to Lora 500/18/1.7 while control chrome stays at affordance register; 22 edits across 6 files; (2) multi-mark highlights end-to-end — schema migration dropping the S113 one-mark-per-verse unique + adding (user_id, verse_id, color, style) unique, API ON CONFLICT flipped, PWA picker rebuilt with chips + cap-aware UX at 3-mark cap, verse render layered via nested spans with per-array-index text-underline-offset for multi-underline stacking, DESIGN_LANGUAGE.md §8 updated. Plus three small UX touch-ups bundled (ThemeToggle "Theme" visible label, picker gear copy "+ Add color labels", free-tier Labels-with-price button hidden to kill every-feature-priced impression). Plus a forward-fix follow-up commit landing the migration-loader pattern (Python loader at `restoration-pipeline/_session117_*.py` + matching `api/Dockerfile` COPY line). Disciplines that carry forward: **body-prose vs control-chrome register split** (don't propagate body font uniformly to all chrome — split the audit into the two registers first); **read-the-actual-file before editing** (caught the ChapterCommentary text-[1.025rem] override that a blanket regex would have missed); **Stop-2 diff plan with gate-question on judgment calls** (Yoshi's banner-sizing + thread-card line-height calls); **`tsc -b` clean as load-bearing verification** for className-only edits when vite can't run in sandbox; **every schema-migration session ships THREE artifacts in lockstep** — SQL under `data-schema/migrations/`, Python loader under `restoration-pipeline/_session{N}_*.py` (asyncpg + argparse + dry-run + verify), and matching `COPY restoration-pipeline/_session{N}_*.py /restoration-pipeline/` line in `api/Dockerfile`. See prior REBUILD_STATE versions in git history for full S117 detail.

- **S113-S115 (highlights + Stripe drift + dark-theme + Cormorant body bump, summarized).** Older detail trimmed per the keep-last-1-2 convention. S113 shipped the 13-color × 3-style highlights wheel end-to-end; S114 shipped the `auth.py` DB-wins-over-JWT subscription-tier resolution and surfaced the Cloudflare bot-mode wheel; S115 shipped the Resubscribe CTA in chrome + Manage.tsx and flipped the PWA to the locked DESIGN_LANGUAGE §1 dark-default aesthetic with the 470nm accent + theme toggle + Cormorant 500/18 body bump (Cormorant later swapped to Lora at S116 after fresh-eyes comparison). Disciplines that carry forward: READ the actual file (path, line range, schema column) before submitting edits; READ DESIGN_LANGUAGE.md before any visual / interaction spec; Verbal-Answers-Transcribe at session close; comparison widgets for visual decisions; AskUserQuestion gates for independent decision points; standard git-lock clear sequence (`rm -f .git/index.lock .git/HEAD.lock`). See prior REBUILD_STATE versions in git history if specific S113-S115 detail is needed.

## Historical handoffs

S97-S109 handoffs exist on disk for historical record (if any) but are NOT read at session-open going forward. Read this file + voice skill + the matthew-N.md database-source for the chapter being worked. **For any visual / UX / interaction-design work also read DESIGN_LANGUAGE.md** — it carries S77/S78 locked decisions that this file only summarizes (the S113 mid-session miss was caught and locked into the Recent observations above). Older handoffs only consulted if a specific question arises. **Matthew sweep closed at S109; next sessions pick from the Next wheels section above. Outside-Matthew audit list (1 Corinthians 11 + John 1) remains queued.**
