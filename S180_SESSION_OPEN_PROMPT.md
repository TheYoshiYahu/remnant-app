# S180 Session Open Prompt

Request access to my Desktop folder using `mcp__cowork__request_cowork_directory` with path `~/Desktop`.

Then load the `anthropic-skills:yoshi-voice` skill in full before doing anything else.

Once the skill is loaded, read `~/Desktop/App/S179_CHECKPOINT.md` for the full plan. We're building out cross-references this session — fan out subagents to parallelize. Voice-gate every thread member note against the skill before it lands.
