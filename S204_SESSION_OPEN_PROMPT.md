# S204 — Red Pill the scriptures

Mount `~/Desktop`. Load yoshi-voice. Read `NEXT_SESSION_RED_PILL_SPEC.md`
FIRST (it carries the whole engine — the transfer test, the four claim
classes, the type-specimen cards, the sourcing rule on Trinitarian
lists). Then `S203_BUILD_NOTES.md` for what just shipped. This
session's one job: build the Red Pill.

## Settled — do NOT re-litigate
- My Study is SHIPPED (S203): unified apparatus home, collections
  (flat tags, folder-feel), search + 13-swatch color filter,
  Highlights tab color sections with fill/underline/outline
  sub-groups, Markdown + PDF come-and-see export, 10-note free cap.
  Chrome pill = the NEW eighth metallic register `chrome-metal-
  spectral` (gold was Listen's — Yoshi's catch).
- Tier line (Yoshi): free = basic marks + 10-note cap; Study Notes
  $1.99 = the apparatus. Bookmarks + yellow-fill highlights stay free.
- Red Pill engine (Yoshi, S203 close): the TRANSFER TEST — every NT
  title/act/argument that the Tanakh gives to Yahuah alone, or that
  Yahuah said he himself would do. Four classes in the spec. Good
  Shepherd ↔ Ezekiel 34, Luke 21 ↔ Zechariah 14, Romans 7:1-4
  first-husband-died, John 14:15 ↔ Exodus 20:6 are the type specimens.
- Trinitarian deity-verse lists: optional completeness audit at the
  END only, verse-numbers only, all framing discarded. Never the seed.

## Open with (10-minute sweep)
1. Confirm S203 commits reached origin/main (My Study `6166196`,
   canon.json sync `c7572db`, spectral pill + Red Pill spec).
2. On-device: ❖ My Study both themes; free cap meter; paid search/
   collections/export; export sacred names intact.
3. Alephbet cleanup commit if not yet done (Mac-only deletes).
4. Optional: Matthew 1 citation restore (Acts 4:12, 1 Cor 11:3).

## The job — proof first (earned discipline)
1. Yoshi answers the spec's six open questions (one mark or per-class;
   the red treatment; free-as-proclamation vs tiered; V1 scope;
   extras in V1; reader-facing name).
2. Proof render: marked reader + ONE tap-card (John 8:58 ↔ Exodus
   3:14, both verses quoted in full, stand-alone italics). Sign-off
   BEFORE schema or wiring.
3. Schema (`red_pill_verses`, curated-overlay pattern) + reader toggle
   + card surface. Seed a hand-curated starter set (~20-30 strongest).
4. Minion sweep for V1 scope; checker gates EVERY entry: Red Line #12
   (son of Adam + kaph preserved at Dan 7:13 / Rev 1:13 / 14:14),
   checklist 10 (Formed/Formless — no Trinity assumption, no
   modalism, inherited view engaged in good faith, never argued
   down), quote-don't-cite, sacred names.
5. Loader runs from the Mac (Render external URL), dry-run first.

## Hard constraints (unchanged)
- `tsc -b` / `npm run build` in `~/Desktop/App/app` (package.json is
  in app/, NOT repo root).
- Commit/push/delete from the Mac ONLY. Sandbox writes but cannot
  delete; no index-touching git from the sandbox.
- Terminal blocks for Yoshi: COMMANDS ONLY — no comments, no
  parentheses, nothing that needs trimming before pasting (S203
  lesson — zsh choked on # lines).
- Prod DB: migrations/loaders from the Mac via api/apply_migration.py
  / loader scripts, Yoshi's IP in Render allowlist.

## Backlog behind this
Maps rebuild (`NEXT_SESSION_MAPS_REBUILD_SPEC.md`) · Timeline · Mark
commentary consolidation · cross-reference sweep (paused).
