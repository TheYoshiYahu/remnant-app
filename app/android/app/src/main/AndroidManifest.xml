<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:configChanges="orientation|keyboardHidden|keyboard|screenSize|locale|smallestScreenSize|screenLayout|uiMode|navigation|density"
            android:name=".MainActivity"
            android:label="@string/title_activity_main"
            android:theme="@style/AppTheme.NoActionBarLaunch"
            android:launchMode="singleTask"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

            <!-- App Links — bible.remnantofpromise.org/strongs/{N} deep links
                 land in-app instead of bouncing to Chrome. autoVerify
                 triggers Android to fetch /.well-known/assetlinks.json on
                 install and register the app as the default handler. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="bible.remnantofpromise.org"
                      android:pathPrefix="/strongs/" />
            </intent-filter>

            <!-- S176 — App Link for the sign-in callback. The WordPress
                 rop-sso-bridge plugin's /wp-json/rop/v1/native-auth-callback
                 endpoint 302-redirects to this URL with ?token=<JWT> after
                 the partner authenticates in the system browser. The
                 intent-filter's autoVerify shares the existing
                 .well-known/assetlinks.json binding (same host, same appId,
                 same cert SHA) so no separate verification step is needed —
                 the OS uses the same trust assertion as the /strongs/ filter.
                 The path is an exact match (not a pathPrefix) because the
                 callback only ever takes the bare /auth-callback path with
                 query params; pathPrefix /auth-callback/ would miss the
                 query-only form. -->
            <intent-filter android:autoVerify="true">
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="https"
                      android:host="bible.remnantofpromise.org"
                      android:path="/auth-callback" />
            </intent-filter>

        </activity>

        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.fileprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data
                android:name="android.support.FILE_PROVIDER_PATHS"
                android:resource="@xml/file_paths"></meta-data>
        </provider>
    </application>

    <!-- Permissions -->

    <uses-permission android:name="android.permission.INTERNET" />
</manifest>
